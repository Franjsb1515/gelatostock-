# Puntos de continuidad por versión

Archivo histórico. CLAUDE.md solo lleva las reglas vigentes y el mapa del proyecto; aquí queda el detalle de cada sesión, del más reciente al más antiguo. Se añade una entrada al cerrar cada versión.

## Punto de continuidad 0.42.1
Lee reports/2026-09-22T14-50-00-000Z-auditoria-general-0421.md. Auditoría pedida por el usuario, sin encargo funcional. Método: volcado de las 15 pantallas con work/audit-fase6.cjs sobre una copia de sus datos (salida en work/, fuera de git por los teléfonos) y una sonda del ciclo del día con su gelato CHOCOLOCO sobre otra copia (reports/audit-2026-09-22-v0421-sonda-dia.txt): identidad del día, día cerrado, cambio de valor con drift, reabrir, deshacer, cierre por peso, objetivo de merma y plan. Corregido, solo textos e interfaz: Configuración («Sobre esta versión» en vez de «prototipo… datos de ejemplo… simulados»; «Mantener la sesión al abrir» en Primeros pasos), jerga «producto terminado» fuera de docs/GUIA_USO.md y src/ai-help.cjs (guía regenerada), plurales «1 producto»/«1 movimiento», balance técnico «No disponible» sin fichas. Lección del chat: al reescribir src/ai-help.cjs, la puntuación por palabras cambia el enrutado; la frase del resumen del día con «gelato» capturó «¿Quién inventó el gelato?» (tests/ai.test.cjs) y quitar «producto» de «Solo al aprobar…» dejó fuera esa frase para «¿Cuándo se descuentan los ingredientes al producir?» (34/35): se redactó de otro modo hasta 35/35 sin tocar pruebas. Otra lección: lanzar evaluate-ai antes de subir la versión sobrescribe el JSON de la versión anterior (se restauró reports/ai-chat-v0420.json con git). Hallazgo sin cambio, en TODO 5 [decidir]: una merma de gelato desde Inventario es merma para la Semana y el objetivo de merma, pero ajuste para core/day.ts y ausente en core/sales.ts y core/value.ts. Pruebas: 200, chat 35/35, escritorio 21 PASS con el ejecutable 0.42.1 (reports/desktop-smoke-2026-09-22-v0421.txt).

## Punto de continuidad 0.42.0
Lee reports/2026-09-21T20-10-00-000Z-mensajes-escribir-y-conversacion.md. Encargo del usuario: escribir él a los proveedores desde Mensajes y ver ahí la conversación. Diagnóstico con sus datos en solo lectura: sus cuatro mensajes de WhatsApp venían de su segundo número, autorizado sin proveedor, y por eso nunca entraron en la bandeja (importToInbox solo importa chats con proveedor, porque messages.supplier es obligatorio). Nuevo: POST /api/whatsapp type `supplierMessage` {supplier, text} (comprueba proveedor, número en la ficha y longitud; envía por whatsapp.send, que ya exige canal conectado y chat autorizado; devuelve el sobre); `chats` en el sobre —`bySupplier` (hasta 50 envíos por proveedor, cruzando el número de la ficha con el destinatario del registro del canal), `unlinked` (recibidos de chats autorizados sin proveedor), `connected` y `ready`—, calculado en whatsappChats() de src/server.cjs con `normalize` de whatsapp-store. Interfaz: acción `writeSupplier` en actions.js (si no hay conexión o ningún chat autorizado, avisa y lleva a WhatsApp), `sentBlock` en views-messages.js («LO QUE LE HAS ESCRITO», últimos seis), aviso de chats sin proveedor en la cabecera de Mensajes y, en la pantalla WhatsApp, cada chat dice si entra en Mensajes. Lo enviado NO se convierte en un mensaje de la bandeja ni se lee por reglas: la lectura es solo para lo que contesta el proveedor. Pruebas: 200, una nueva en tests/server.test.cjs; comprobación nueva en scripts/desktop-smoke.cjs; prueba de escritorio **21 PASS con el ejecutable 0.42.0** (reports/desktop-smoke-2026-09-21-v0420.txt), lanzada por el usuario con el equipo despierto; chat 35/35. work/check-mensajes-conversacion.cjs prepara el canal de una copia con WhatsAppStore y recorre la pantalla (salida en work/check-mensajes-conversacion.txt).

## Punto de continuidad 0.41.0
Lee reports/2026-09-21T18-40-00-000Z-valor-de-venta-fechado.md. El valor de venta se puede fechar: el formulario del Recetario (src/ui/actions.js, acción setSaleValue) trae «Vale desde» con hoy por defecto y un año de margen a cada lado. El núcleo ya aceptaba `from` desde la 0.30.0 y `saleValueOn` ya elegía la entrada con el `from` mayor que no pasa del día mirado: lo que faltaba era elegirlo en pantalla. `setSaleValue` añade a la nota cuántos días cerrados quedan desde esa fecha (su instantánea no cambia y el día avisa del cambio, como desde la 0.31.0). Ojo con una corrección propia de esta sesión: se llegó a prohibir fechar hacia delante y tests/day.test.cjs lo cazó —fechar hacia delante estaba soportado a propósito («desde el lunes subo»)—; se quitó la prohibición en vez de tocar la prueba. El precio de compra sigue sin fecharse y queda explicado en TODO por qué no es el mismo caso. Antes de esta versión se cerró el pendiente de la prueba de escritorio: 21 PASS con el ejecutable 0.40.2 (reports/desktop-smoke-2026-09-21-v0402.txt). Pruebas: 199, una nueva en tests/value.test.cjs; comprobación nueva en scripts/desktop-smoke.cjs que todavía no se ha visto pasar (la prueba de escritorio se cortó dos veces en `page.screenshot: Timeout`, pantalla suspendida); chat 34/34 con un caso nuevo. work/check-valor-desde.cjs recorre la pantalla sobre una copia de data/ (salida en work/check-valor-desde.txt).

## Punto de continuidad 0.40.2
El usuario: «cuando cierro y abro la app, el whatsapp se desconecta». La sesión nunca se perdía (close() cierra el navegador, no hace logout; los cuatro `data/whatsapp/sessions/session-*` siguen ahí), pero `autoconnect` estaba en «0» en su whatsapp.sqlite, así que la app arrancaba desconectada: reconectar al abrir era un interruptor aparte. Ahora `rememberSession()` se llama en el evento `ready` (enciende autoconnect al conectar) y `disconnect()` lo apaga, que es la única vía por la que la persona cierra la sesión. `migrateRememberedSession()` corre en el constructor una sola vez (marca `autoconnect_v2` en meta): enciende el recuerdo si hay `active` y existe la carpeta `sessions/session-<id>`; si la persona lo apaga después, se respeta. Interfaz: el botón pasa de «Conectar al abrir» a «Mantener la sesión al abrir» y la letra pequeña explica que cerrar la app no cierra la sesión. Dos pruebas nuevas en tests/whatsapp.test.cjs (recordar y olvidar; migrar una sola vez y respetar el apagado). Comprobado sobre una copia de data/whatsapp: al instanciar la conexión, autoConnect pasa a true con su número vinculado. 198 pruebas, chat 33/33.

## Punto de continuidad 0.40.1
El usuario no podía abrir la app: «ABRIR GELATOSTOCK.vbs» tenía la ruta `dist\GelatoStock-0.36.0-win32-x64\GelatoStock.exe` escrita a mano y scripts/package.cjs borra la carpeta de la versión anterior al empaquetar (error 80070002 de Windows Script Host). El acceso recorre ahora dist y arranca el GelatoStock.exe de la carpeta modificada más tarde; sin ninguna, muestra un MsgBox que dice qué hacer. tests/launcher.test.cjs falla si alguien vuelve a escribir una versión concreta ahí. Comprobado en seco (una copia del .vbs que escribe la ruta elegida en vez de arrancar): antes de empaquetar elegía 0.40.0 y después de empaquetar la 0.40.1 elige 0.40.1 sin tocar nada. El ejecutable 0.40.1 arranca con un perfil temporal. 196 pruebas.

## Punto de continuidad 0.40.0
Lee reports/2026-09-21T15-30-00-000Z-objetivo-de-merma-por-producto.md. Decisión del usuario del 2026-09-21: objetivo de merma por producto, en su unidad y también en porcentaje. Núcleo: `wasteGoal {mode: quantity|pct, value, at}` en productSchema **fuera de productFields** (editar la ficha no lo borra, como el valor de venta de una receta); acción `setWasteGoal {product, mode, value}` en core/domain.ts (value 0 lo quita; rechaza un porcentaje mayor que 100); `wasteGoals(state, días = 7, hoy)` en core/sales.ts devuelve por producto la merma y «lo que salió» de esos días (movimientos vivos con delta negativo, kind count excluido: un ajuste de inventario no es una salida), el porcentaje derivado (null si no salió nada) y si se pasó; el día de un movimiento de cierre es su día de negocio y el de uno de inventario, su día natural. Servidor: viaja en el sobre como `alerts.waste`. Interfaz: botón por fila en Inventario (`data-action="setWasteGoal"`), modal con el estado de esos 7 días y la fórmula, pastilla «Merma por encima» en la fila y aviso en el inicio (`wasteOverText` en views.js). Trampa encontrada: src/ui/actions.js manda a WhatsApp toda acción que empiece por «wa», así que la acción no puede llamarse «wasteGoal»; queda comentado ahí y en CLAUDE.md. Pruebas: 195, una nueva en tests/sales.test.cjs (unidad y porcentaje, conteo que no cuenta, merma deshecha, límites y que editar la ficha no borra el objetivo); comprobación nueva en scripts/desktop-smoke.cjs (pasó, pero la prueba de escritorio completa no termina en este equipo: se corta en `page.screenshot: Timeout`, la pantalla suspendida que documenta CLAUDE.md; repetirla con el equipo despierto); chat 32/32 con un caso nuevo. work/check-objetivo-merma.cjs recorre la pantalla sobre una copia de data/ (salida en work/check-objetivo-merma.txt). Pendiente: la fase 4 del plan de compras (tarifas reales del usuario) y sus correcciones de uso.

## Punto de continuidad 0.39.0
Lee reports/2026-09-21T14-20-00-000Z-mermas-de-ingredientes-con-motivo.md. Decisión del usuario del 2026-09-21: la merma de un ingrediente usa los mismos motivos que el cierre del día. Núcleo: `stockWasteText(reason, detail?)` en core/sales.ts escribe «Merma · Motivo[ · detalle]» —nunca «Merma del día», para que closeLineOf no lo lea como línea de cierre— y `wasteLabelOf(m)` da la etiqueta del motivo de cualquier merma viva (del cierre o de Inventario), «Sin motivo» si el texto no lo dice y null si no es merma (un texto antiguo de degustación es invitación, no merma). La acción `movement` gana `wasteReason` opcional; su `reason` ya no es obligatorio en el esquema y lo exige core/domain.ts (rechaza motivo de merma en una entrada o salida, y un movimiento sin motivo ni texto). La lista de motivos, que estaba repetida tres veces en core/schema.ts, es ahora una constante (`wasteReasonEnum`). core/report.ts añade `wasteByReason` al resumen semanal: agrupa por etiqueta las mermas de la semana, con sus líneas por producto y el número de apuntes, sin sumar cantidades entre unidades distintas. El sobre de estado lleva `wasteReasons` (las etiquetas) para el formulario. Interfaz: el modal de «Entrada / salida» (src/ui/actions-extended.js) enseña «Motivo de la merma» solo con kind=waste y convierte el texto libre en «Detalle (opcional)»; regla nueva `.field[hidden]` en styles.css; bloque «Mermas por motivo» en src/ui/views-weekly.js. Pruebas: 194, una nueva en tests/sales.test.cjs que cubre texto, actividad, rechazos, merma antigua y la agrupación semanal; una comprobación nueva dentro del ejecutable en scripts/desktop-smoke.cjs; chat 31/31 con un caso nuevo. work/check-merma-motivos.cjs recorre la pantalla sobre una copia de data/ (salida en work/check-merma-motivos.txt). Pendiente: el objetivo de merma con aviso (falta acordar cómo se mide) y la fase 4 del plan de compras (tarifas reales del usuario).

## Punto de continuidad 0.38.0
Lee reports/2026-09-21T12-55-00-000Z-aviso-de-mensajes-nuevos.md. El usuario no mandó tarifas, así que la fase 4 del plan sigue parada y esta versión sale de TODO §3. Corrección de TODO verificada en esta sesión: las reglas NO se aplicaban «al abrir la bandeja», sino al importar (importToInbox de src/whatsapp.cjs dispara la acción `message` y classify de core/domain.ts lee ahí mismo); lo que faltaba era enterarse. Nuevo: `GET /api/pulse` en src/server.cjs (cookie y Origin como el resto; devuelve revision, notices, unread, toRead y la lectura del último mensaje —categoría, etiqueta, resumen y needsReading—, nunca su texto); ajuste `message_notices_off` por /api/maintenance type `messageNotices`, en el sobre como `messageNotices`. src/whatsapp.cjs: importToInbox devuelve la lectura que escribió el núcleo y el evento `message` se emite después de importar para llevarla; src/desktop.cjs la pone en la notificación de Windows y respeta el interruptor. Interfaz (src/ui/core.js): pulse() cada 5 s —parado con modal abierto o acción en curso—, `#incoming` se escribe a mano sin render() para no perder lo que se esté escribiendo, `staleState` y nav() refrescan el estado al cambiar de pantalla, applyEnvelope marca visto el mensaje más nuevo; acciones incomingOpen/incomingHide en actions.js, interruptor en events.js (#message-notices) y tarjeta en views.js. Estilos con clases en styles.css (#incoming, .incoming-bar). Si cambias el formato del sobre o del latido, recuerda que el aviso vive fuera del render. Pruebas: 193, una nueva en tests/server.test.cjs (el latido) y otra en tests/whatsapp.test.cjs (el evento lleva la lectura); 21 PASS de escritorio (mismos bloques) con una comprobación nueva dentro del ejecutable; chat 30/30 con dos casos nuevos. work/check-aviso-mensajes.cjs recorre la fase sobre una copia de data/ (salida en work/check-aviso-mensajes.txt). Pendiente: fase 4 del plan (tarifas reales del usuario) y mermas de ingredientes con motivos, que él tiene que decidir.

## Punto de continuidad 0.37.0
Lee docs/PLAN_COMPRAS_MENSAJES_II.md y reports/2026-09-20T23-41-17-402Z-fase-5-catalogo-y-precios-por-proveedor.md. Fase 5: catálogo y precios por proveedor. Núcleo: `readPriceChange` en core/messages.ts (reglas estrechas a propósito: exige palabra de precio y o bien «de X € a Y €» o bien un único importe en euros; con dos importes sueltos o un porcentaje no propone nada) y tres campos nuevos en la interpretación (priceTo, priceFrom, priceHint, también en el schema); `ref` opcional en cada entrada de `prices` (el documento o el mensaje que la justifica); acción `setPrice {product, price, source: document|message, ref}` en core/domain.ts, que exige que la cita exista y sea del mismo proveedor, rechaza el precio que ya tiene la ficha, escribe la entrada del historial y actualiza el precio; `supplierCatalog`/`supplierCatalogs` en core/inventory.ts, donde la fecha y el origen solo se dan si la última entrada de ese producto y proveedor coincide con el precio actual (si no, null → «No disponible»), e incluye como `usual:false` los productos que ese proveedor surte como alternativo. Servidor: el catálogo viaja en el sobre de estado (`catalog`). Interfaz: botón «Qué le compras» en la ficha del proveedor (supplierCatalogModal en actions-extended.js), bloque «Precio que dice el mensaje» con «Apuntar este precio…» (priceActions en views-messages.js), «Apuntar un precio» en cada documento con proveedor, y setPriceModal para elegir producto y repasar el importe. Corrección de paso: src/ui/events.js leía el sobre a mano en la primera carga, así que avisos, plantillas y catálogo estaban vacíos hasta el primer cambio; ahora usa applyEnvelope. Pruebas: seis casos nuevos en tests/domain.test.cjs, uno en tests/server.test.cjs, una comprobación dentro del ejecutable en scripts/desktop-smoke.cjs y dos casos de chat (28/28). work/check-fase5-cm2.cjs recorre la fase sobre una copia de data/ (salida en work/check-fase5-cm2.txt). Pendiente del plan: solo la fase 4 (listas de precios en foto), parada hasta que el usuario mande tarifas reales; sigue diferido quitar el vocabulario de «simulación».

## Punto de continuidad 0.36.0
Lee docs/PLAN_COMPRAS_MENSAJES_II.md y reports/2026-09-20T22-37-59-473Z-fase-3-texto-de-los-pdf.md. Fase 3: texto de los PDF con la dependencia `unpdf` (2,4 MB en node_modules, +2,1 MB en el ejecutable sobre 1.530 MB; pdf.js empaquetado, sin red y sin binarios nativos). src/pdftext.cjs (readPdfText) acepta un data URL o un Buffer, exige cabecera %PDF-, corta en 10 MB, 20.000 caracteres y 30 segundos, y devuelve {text, pages, truncated, reason}: sin texto, `reason` explica que es un escaneo y `text` queda vacío, nunca se rellena. Servidor: POST /api/pdf (con cookie y Origin, cuerpo hasta 14 MB) devuelve la lectura y la propuesta de identifySupplier; whatsapp.pdfText lee los adjuntos PDF al importarlos (src/whatsapp.cjs, importDocument). Interfaz: el formulario de Documentos lee el PDF al elegirlo y rellena «Texto leído del documento»; si no hay texto enseña el motivo. Pruebas: tests/pdftext.test.cjs con dos PDF de tests/fixtures impresos por Chromium (work/make-pdf-fixture.cjs), uno con texto y otro solo con imagen; el servidor y WhatsApp tienen su caso; la prueba de escritorio comprueba la lectura dentro del ejecutable. work/check-fase3-cm2.cjs recorre la fase sobre una copia de data/. Pendiente del plan: 4 listas de precios en foto (hace falta que el usuario mande tarifas reales) y 5 catálogo y precios por proveedor.

## Punto de continuidad 0.35.0
Lee docs/PLAN_COMPRAS_MENSAJES_II.md y reports/2026-09-20T22-11-20-102Z-fase-2-reclamar-respuesta-y-plantillas.md. Fase 2: recordatorio y textos editables. Núcleo: `nudges` (array de dispatchSchema) en orderSchema y acción `nudge` (exige pedido sent o partial, con dispatch, sin confirmedAt y sin otro recordatorio el mismo día según localDate; solo añade el registro). core/orders.ts añade defaultNudgeTemplate, renderNudgeTemplate (ignora una plantilla sin {numero}), nudgeMessage y daysSinceDispatch. Servidor: POST /api/whatsapp con type nudgePreview y nudge (las mismas comprobaciones antes de enviar, para no mandar un mensaje que la acción luego rechace; el texto lo escribe la persona, 1 a 1.000 caracteres; whatsapp.send sin `order` porque ese hueco es del envío del pedido), y /api/maintenance con nudgeTemplate y replyTemplates; ambos viajan en el sobre de estado (nudgeTemplate, replyTemplates). Interfaz: botón «Reclamar respuesta» en la ficha del pedido (se apaga con nudgedToday), acción orderNudge con el texto editable, atajo en el aviso del inicio, tarjetas nuevas en Configuración y replyDefaults en views-messages.js (ocho botones con id; {fecha} se sustituye con la fecha leída). La prueba del servidor cubre el ciclo con cliente falso; el doble de WhatsApp ahora devuelve un id por mensaje y el diagnóstico del canal registra dos envíos (pedido y recordatorio). work/check-fase2-cm2.cjs recorre la fase sobre una copia de data/ con un envío ficticio propio de la copia. Siguientes del plan: 3 texto de los PDF, 4 listas de precios en foto, 5 catálogo y precios por proveedor.

## Punto de continuidad 0.34.0
Lee docs/PLAN_COMPRAS_MENSAJES_II.md y reports/2026-09-20T21-35-26-886Z-fase-1-otro-proveedor-del-mismo-producto.md. Fase 1 del plan nuevo: `alternates` en productFields (proveedor, pack y price escritos por la persona, hasta cinco, nunca deducidos), `supplier?` opcional en cada línea del carrito y en la acción `cart` (sin ese campo la línea conserva el proveedor elegido; nombrar al habitual lo devuelve), acciones `setAlternate` y `removeAlternate` (quitar uno limpia las líneas del carrito que lo usaban), y `authorize` agrupa por el proveedor de la línea tomando pack y price del alternativo. Interfaz: botón «Otros proveedores» en cada fila de Inventario (altSuppliersModal en actions-extended.js), desplegable por línea en el carrito (cartSupply y cartSupplierPicker en views-orders.js, evento data-cart-supplier en events.js) y botones «Comprarlo a …» en un mensaje de falta de producto (orderActions en views-messages.js, acción buyElsewhere). Sin migración de SQLite: productos y carrito guardan JSON y zod pone los valores por omisión. De paso: fuera «unidad base» de los formularios (unitLabel y retitleField en forms.js; la prueba de escritorio busca los nombres nuevos), «Proveedor de demostración» en el alta y el plural de «1 pedidos». work/check-fase1-cm2.cjs recorre la fase sobre una copia de data/ (salida en work/check-fase1-cm2.txt). Queda pendiente del plan: reclamar respuesta (fase 2), texto de los PDF (3), listas de precios en foto (4) y catálogo por proveedor (5); el vocabulario de «simulación» se conserva por decisión del usuario.

## Punto de continuidad 0.33.0
Lee reports/2026-09-20T19-56-48-598Z-fase-6-repaso-de-claridad.md. Fase 6: repaso de claridad hecho leyendo cada pantalla sobre una copia de los datos reales (work/audit-fase6.cjs escribe work/audit-fase6.txt). Hallazgo principal: el usuario había producido su gelato antes de activarlo y esos kilos no entraron en ningún stock (una producción sin recipe.product no tiene output). Ahora createFinished abre un modal que lo explica, pregunta «Kilos que tienes ahora» y, si se escriben, registra un conteo con el motivo «Kilos que había al activar el gelato» (en el resumen del día sale como ajuste de inventario, que es lo que es). Una propuesta de producción de un gelato sin activar avisa y trae el botón; para la familia «base» el texto es neutro. Textos de demostración sustituidos en inicio, Inventario, Compras y en la nota de authorize; «producto terminado» fuera también de los mensajes del núcleo y del motivo del movimiento de entrada («Gelato hecho: …», no lo lee nadie por texto). Quedan «unidad base» en formularios y el vocabulario «simulación» de Compras (TODO).

## Punto de continuidad 0.32.0
Lee reports/2026-09-19T18-30-44-856Z-fase-5-que-producir-hoy.md. Fase 5 del plan: core/plan.ts (businessDay, productionPlan, todayBrief). Falta = objetivo − stock (mínimo 0; null sin objetivo); venta media = kilos vendidos en los 14 días anteriores a hoy ÷ días con algún cierre en ese periodo; «da para» = stock ÷ media. Acción setGoal {product, target}: solo gelatos de una receta; baja el mínimo si quedara por encima (validate exige objetivo ≥ mínimo). GET /api/plan; alerts.day (todayBrief) viaja en cada sobre de estado. Interfaz: planPanel en views-production.js (planData se carga en loadSales junto con historial y día), homeDay en views.js, acción openDay (abre Producción con el resumen en un día dado), y el modal de producir admite data-quantity. Evaluación del chat: 23 casos. work/check-fase5-copia.cjs recorre la fase sobre una copia de data/.

## Punto de continuidad 0.31.1
Lee reports/2026-09-19T17-28-50-824Z-gelato-dado-de-alta.md. Corrección tras el primer uso real: la receta del usuario no tenía recipe.product, así que ni valor de venta ni cierre del día funcionaban, y la pantalla no lo explicaba. La acción recipe acepta createProduct: crea el producto (kg, stock 0, mínimo y objetivo 0, precio 0, zona vitrina) con el nombre de la receta y el proveedor interno ownSupplierId «Elaboración propia» (se crea si falta); reutiliza un producto en kg con el mismo nombre si no es ingrediente. Interfaz: el editor de recetas nuevas lo trae elegido; botón createFinished («Activar ventas y valor de este gelato») en la ficha y en el cierre vacío. «Producto terminado» ya no aparece en la interfaz. Evaluación del chat: 22 casos. work/check-copia-real.cjs recorre el camino completo sobre una copia de data/.

## Punto de continuidad 0.31.0
Lee reports/2026-09-19T16-53-44-929Z-fase-4-cuanto-debi-vender.md. Fase 4 del plan: core/day.ts (computeDay, daySummary, isDayClosed). El día de negocio de un movimiento es el de su cierre (closeLineOf), el de su producción, o su hora menos day_change_hour; lo compensado no cuenta. «Queda para mañana» de un día = stock actual − movimientos de días posteriores; «al empezar» sale de la identidad. Estado nuevo s.days (tabla SQLite days, user_version 5; una base 4 abre sin migrar nada porque la tabla se crea vacía): {id day-AAAA-MM-DD, date, status closed|reopened, confirmedAt, realSaleCents?, snapshot, log}. El orden de las claves de la instantánea en schema.ts es el de day.ts a propósito: drift compara por JSON. Acciones confirmDay (el servidor inyecta changeHour; la venta real no se arrastra entre cierres) y reopenDay (motivo obligatorio). ensureDayOpen protege dailySales, undoDailySales, undoCloseLine, editCloseLine, applyProduction y voidProduction. GET /api/day?date=. Interfaz: dayPanel y dayIsClosed en views-sales.js; loadSales carga historial y día a la vez, así que salesData = null refresca ambos (ahora también tras aprobar una producción). ai-help.cjs: frases de 450 caracteres como máximo. Evaluación del chat: 21 casos.

## Punto de continuidad 0.30.0
Lee reports/2026-09-19T16-12-47-768Z-fase-3-valor-y-coste.md. Fase 3 del plan: core/value.ts (recipeCost, productionCost, saleValueOn, costPerKgOn, valueReport). La receta gana saleValues (historial {from, cents, at}; el valor de un día es la entrada con mayor from ≤ día) y manualCost; van en recipeSchema y no en recipeFields, para que editar la receta no los borre. La producción gana cost {cents, source}, instantánea al aprobar: coste a mano × kilos si existe, si no Σ(consumo aprobado × precio/presentación), y ausente si falta algún precio. Acciones setSaleValue (mismo día: sustituye; exige producto terminado; un solo dueño del valor por producto) y setManualCost (sin cents lo quita); ambas bloqueadas por el candado del recetario. Sin migración de SQLite: todo viaja en el JSON de recetas y producciones. El servidor añade cost a cada receta desbloqueada y value (valueReport) a GET /api/sales. Coste por kilo de un día = última producción viva con coste hasta ese día; si no hay, coste actual de la receta. Un null anula el total (no se suman parciales). Interfaz: valueBlock en views-production.js, valuePanels en views-sales.js. Ojo con src/ai-help.cjs: el buscador del chat descarta frases de más de 450 caracteres; escribe frases cortas con etiqueta. La evaluación del chat tiene ahora 20 casos.

## Punto de continuidad 0.29.0
Lee reports/2026-09-18T21-52-07-524Z-fase-2-merma-e-invitacion.md. Fase 2 del plan: tercera categoría de salida en el cierre, «invitación o consumo» (campo gift de cada línea de dailySales; movimiento exit con texto «Invitación o consumo del día AAAA-MM-DD»). core/sales.ts se reescribió alrededor de closeLineOf, que clasifica cualquier movimiento de cierre en sale, waste o gift; salesHistory, closeMovements, report.ts y las acciones de corregir y eliminar línea pasan por ahí. «tasting» dejó de ser un motivo de merma; los movimientos antiguos con «· Degustación o invitación» no se reescriben: closeLineOf los lee como gift. Porcentaje de merma = merma / (vendido + merma + invitación). WeeklyReport gana gifts, days[].gift y totals.gifts.

## Punto de continuidad 0.28.0
Lee reports/2026-09-18T21-42-03-735Z-fase-1-corregir-sin-miedo.md y docs/PLAN_PRODUCCION_VENTAS_CAJA.md (fase 1 hecha). Acciones nuevas en core/domain.ts: voidProduction (compensa los movimientos vivos de una producción aplicada, primero el producto terminado y luego los ingredientes; se bloquea con explicación si ya no queda ese producto; con redo deja una propuesta igual), undoCloseLine y editCloseLine (una línea de un cierre; editar = compensar y registrar de nuevo con el mismo día y, salvo que se cambie, el mismo motivo). Una producción anulada queda con status "discarded" más voidedAt y voidReason: así no hubo que migrar la restricción CHECK de SQLite; validate acepta movimientos de una producción anulada. salesHistory devuelve las líneas vivas de cada día (CloseLine). Ajuste day_change_hour (0–8, por defecto 5): la interfaz usa businessToday() para proponer fechas; todayLocal() sigue siendo el día real. Pesos en gramos solo en la interfaz (toKg); el núcleo sigue en kg. button.text-link tiene ahora estilo propio en toda la app.

## Punto de continuidad 0.27.0
Lee reports/2026-09-18T16-58-36-020Z-cierre-del-dia.md. Cierre del día: core/sales.ts (motivos de merma, closeMovements, salesHistory), acción dailySales con sold o remaining y wasteReason, acción undoDailySales, GET /api/sales, src/ui/views-sales.js (la sección salió de views-production.js). El día de negocio y el motivo viajan en el texto del movimiento («Venta del día AAAA-MM-DD», «Merma del día AAAA-MM-DD · Motivo»): no cambies ese formato sin migrar core/sales.ts, core/report.ts y core/context.ts, que lo leen. Un movimiento compensado nunca cuenta en historial, semana ni «Ventas e impacto». El formulario recalcula sin redibujar (updateSalesSummary) para no perder el foco. Siguiente: confirmar con el usuario la segunda parte de ventas y mermas (ver TODO) y Compras y Mensajes II.

## Punto de continuidad 0.26.0
Lee reports/2026-09-18T16-30-14-697Z-contexto-del-dia.md. Contexto del día: core/context.ts (parseMetno por día de Palma, parseHolidayCsv del CSV catalán del Govern, salesByImpact y dailySalesKg), src/context/providers.cjs (MET Norway y CKAN de la CAIB) y src/context/service.cjs (data/contexto.sqlite: weather, holidays, events, meta). Mismo interruptor que cruceros (cruises_off) y mismas garantías: el clima de un día pasado es «previsión guardada», nunca observación; una previsión parcial no pisa una más completa del mismo día; lluvia sin dato = null, no 0; festivos solo del CSV oficial, y un CSV raro no borra los buenos; eventos solo manuales; «Ventas e impacto» no calcula correlaciones y exige 8 días por nivel (MIN_DAYS_PER_LEVEL) para dar una media por buena; una venta deshecha no cuenta. createApp({ cruiseProvider, contextProviders }) en pruebas: ninguna toca la red. Ojo de herramienta: los heredoc y node -e pierden las barras invertidas de las expresiones regulares; usa Write o Edit. Siguiente: mejora de ventas y mermas (el usuario quiere definirla) y Compras y Mensajes II.

## Punto de continuidad 0.25.0
Lee reports/2026-09-17T23-07-26-548Z-cruceros-planificacion.md y el prompt del usuario para este módulo (regla de oro: no inventar datos). Cruceros: core/cruises.ts (todo lo puro: validación de filas de la APB con posiciones en COL, hora Europe/Madrid, daySummary, impacto), src/cruises/provider-apb.cjs (HTTP), repository.cjs (data/cruceros.sqlite: ships, calls, changes, syncs, meta) y service.cjs (sync, backoff, backfill, vistas). Sustituye a src/cruises.cjs y cruceros.json de 0.24.0. Garantías que no se degradan: null = «No disponible» y nunca se rellena; «pasajeros declarados» es DERIVADO (tránsito + máx(desembarque, embarque)) y no es capacidad ni clientes; naviera y capacidad solo manuales y con fuente; estados solo los de la fuente más «Retirada de la previsión» y «Salida prevista ya pasada», ambos derivados y rotulados; «hoy» y todas las horas son de Palma (el equipo de desarrollo está en UTC−3); una previsión vacía nunca retira escalas; el histórico no se borra. La fórmula del impacto está en impactLevel y en la propia pantalla: si cambia, cambian las dos y las pruebas. Pruebas con proveedor simulado (createApp({ cruiseProvider })); ninguna toca la red. Siguiente: Compras y Mensajes II (ver TODO).

## Punto de continuidad 0.24.0
Lee reports/2026-09-17T22-17-03-972Z-cruceros-palma.md. El proyecto vive ahora en D:/CARPETAPROYECTOS/APPGELATOSTOCK. Cruceros: src/cruises.cjs (parseCalls puro, CruiseRegistry con fetcher inyectable, data/cruceros.json derivado), rutas GET/POST /api/cruises, ajuste cruises_off, envelope.cruises, src/ui/views-cruises.js. Es la única salida a internet además de WhatsApp: solo lectura, cuerpo fijo, respuesta tratada como no fiable, nunca automática bajo node:test ni con GELATO_CRUISES_AUTO=0 (la prueba de escritorio lo usa). El servicio del puerto no es una API documentada: las posiciones de columna están en COL; si cambian, corrige ahí y en tests/cruises.test.cjs. Citar siempre «Origen de los datos: Autoridad Portuaria de Baleares». La interfaz no admite estilos en línea (CSP): usa clases. Siguiente: Compras y Mensajes II (ver TODO).

## Punto de continuidad 0.9.3
Lee reports/2026-09-08T18-40-07-473Z-diagnostico-whatsapp.md. Sesión 014: el usuario reportó que con QR conectado no llegaba nada en ninguna dirección (canal sin mensajes ni envíos). Se añadió resolución de remitentes LID, diagnóstico local del canal (data/whatsapp/diagnostico.log, visible en la app) y envío de prueba a chats autorizados. El registro real mostró recepción correcta (descartes por autorización de otra cuenta: cada cuenta vinculada tiene su lista) y envío silenciosamente fallido por chat inexistente para número@c.us; corregido resolviendo el id con getNumberId/LID y fallando de forma visible. Pendiente que el usuario repita la prueba.

## Punto de continuidad 0.9.1
Lee reports/2026-09-08T18-16-51-221Z-envio-whatsapp-real.md. Sesión 013: envío real por WhatsApp (src/whatsapp.cjs send, tabla sent, acción send con dispatch, /api/whatsapp preview/send, botón en Control de entregas). Probado con cliente simulado; la prueba con teléfono real la hace el usuario. 85 pruebas.

## Punto de continuidad 0.9.0
Lee reports/2026-09-08T16-18-04-876Z-recetas-produccion-respuestas.md. Sesión 012: recetas/producción (consumo estimado por reglas, aprobado y corregible por la persona; producto terminado; hoja diaria; SQLite user_version 2) y lectura de respuestas de proveedores por reglas en core/messages.ts con segunda lectura opcional del modelo (acción aiNote, solo anota). Precisión en respuestas: reglas afinadas con corpus de 80 (60 desarrollo + 20 reservado, 75 % en la primera pasada) son la lectura principal; los modelos locales miden 55/80 (0.6B) y 36/80 (1.7B) y son manipulables: no sustituir reglas por modelo sin cifras. Auditoría de seguridad con sonda work/attack.cjs 26/26; clave fuera de la URL; caché validada del estado. Decisiones del usuario: descuento solo tras aprobación; producto terminado sí; la IA debe entender respuestas libres, por eso reglas + modelo; QR de WhatsApp aplazado. 80 pruebas.

## Punto de continuidad 0.8.2
Lee reports/2026-09-08T15-51-40-496Z-chat-dudas-ia.md y, antes, reports/2026-09-08T15-15-49-334Z-auditoria-prompt-maestro.md. Sesión 011 añadió el chat de dudas (src/ai-help.cjs guía fija; src/ai.cjs chat(); ruta /api/ai/chat) sin acceso a datos ni acciones. Un intento de reforzar los prompts para proforma/abono empeoró la evaluación completa (7/11 frente a 10/11): se conservan los prompts originales; reports/ai-evaluation-v082.json es la línea base de 11 casos y ai-evaluation-v082-prompts-explicitos.json el intento descartado. Cambiar prompts exige repetir esa evaluación. La versión ya se lee de package.json en servidor e interfaz. 72 pruebas.

 Sesión 010 auditó la entrega 0.8.0 de ChatGPT: parche de commit idéntico a 452dcc4, 69/69 pruebas y formato correctos, código de IA revisado. Cambios mínimos: etiquetas habituales de IVA/total en la comprobación aritmética (menos abstenciones sin ampliar interpretación), bloqueo adicional de tls/http2/dgram/dns en el trabajador (sigue sin ser sandbox de SO) y nombre de evaluación por versión. 70 pruebas.

Q4 fue elegido tras comparar variantes: no asumir que un modelo mayor será mejor en este equipo. Experimentos descartados quedan en work/runtime de desarrollo, excluidos del ejecutable y ZIP de fuentes; scripts/package.cjs copia solo el modelo definido en el manifiesto. No incluir modelos ajenos al manifiesto al empaquetar.

Doble lectura del mismo modelo con prompts distintos, sin independencia estadística. src/ai-review.cjs contiene reglas, NO razonamiento neuronal. La coincidencia no da permiso para actuar. Control monetario limitado a un esquema base/cuota/total sin otros conceptos; no es validación fiscal.

Evaluación completa 0.8.2: 10/11 con los prompts vigentes, ningún error aceptado; proforma queda Por revisar y el archivo conserva pass:false deliberadamente; no ocultar abstenciones ni llamar a eso exactitud perfecta. Pruebas de seguridad/funcionamiento son distintas de métricas de calidad de clasificación. Sigue pendiente razonamiento general avanzado y corpus representativo.

El usuario pide respuestas MUY breves en chat; informes técnicos completos en estos documentos. Antes de cambiar archivos, comprobar git status para evitar pisar cambios de otro colaborador.

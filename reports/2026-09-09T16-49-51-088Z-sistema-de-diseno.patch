diff --git a/ABRIR GELATOSTOCK.vbs b/ABRIR GELATOSTOCK.vbs
index 0842282..27c38cc 100644
--- a/ABRIR GELATOSTOCK.vbs	
+++ b/ABRIR GELATOSTOCK.vbs	
@@ -5,4 +5,4 @@ shell.CurrentDirectory = root
 shell.Environment("PROCESS")("GELATO_DATA_DIR") = root & "\data"
 shell.Environment("PROCESS")("TEMP") = root & "\work"
 shell.Environment("PROCESS")("TMP") = root & "\work"
-shell.Run Chr(34) & root & "\dist\GelatoStock-0.14.0-win32-x64\GelatoStock.exe" & Chr(34), 1, False
+shell.Run Chr(34) & root & "\dist\GelatoStock-0.14.1-win32-x64\GelatoStock.exe" & Chr(34), 1, False
diff --git a/CHANGELOG.md b/CHANGELOG.md
index f8e374f..c183c6e 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,11 @@
 # Registro de parches y sesiones
 
+## 0.14.1 — 2026-09-09 · sesión 024
+
+Sistema de diseño completo, ejecutado por un agente de diseño (Fable 5.1) con el encargo docs/PROMPT_DISENO.md: tokens en :root (paleta Mediterraneo con variantes AA, escala 12/13/14/15/18/22/32, interlineados, ritmo de 8 px, anchos de lectura, radios, sombras, alturas de control), componentes con estados, acento por pantalla, navegación de 38 px, diálogos con pie fijo, tablas legibles y estados vacíos con guía. Corregidos los solapamientos de las tarjetas de Documentos y 1.165 hallazgos de la auditoría DOM (textos < 11 px, contraste, cortes, alturas) hasta 0 en 73 vistas y 4 tamaños. Documentado en docs/DISENO_SISTEMA.md; capturas antes/después en output/design y muestras en reports/design.
+
+110 pruebas, ejecutable 0.14.1. Informe reports/2026-09-09T16-49-51-088Z-sistema-de-diseno.md.
+
 ## 0.14.0 — 2026-09-09 · sesión 023
 
 Documentos por proveedor: pantalla nueva con facturas, albaranes, recibos y pedidos (fotos y PDF hasta 10 MB), filtros por proveedor, sin pedido, con propuesta y PDF. Propuestas por reglas de proveedor, tipo y pedido (número de pedido en el texto; único pedido del proveedor en 60 días; importe del documento frente al estimado del pedido ±5 %) que la persona acepta con un clic; vínculo manual a cualquier pedido del proveedor y desvinculación. Los adjuntos de WhatsApp de proveedores autorizados se archivan solos con OCR local si son imágenes. Control de entregas muestra los documentos de cada pedido.
diff --git a/CLAUDE.md b/CLAUDE.md
index b35722e..25bb703 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -1,4 +1,4 @@
-# Entrada para Claude · entrega vigente 0.14.0
+# Entrada para Claude · entrega vigente 0.14.1
 
 Lee docs/PROMPT_MAESTRO_V2.md (prompt operativo), AGENTS.md, README.md, TODO.md, el último informe de reports/ y docs/IA_LOCAL_Y_SEGURIDAD.md. El usuario solicita revisión y mejoras justificadas, manteniendo trabajo en D y funcionamiento local. Ejecuta pruebas antes de proponer reescrituras.
 
diff --git a/README.md b/README.md
index 4829aa5..c48c07d 100644
--- a/README.md
+++ b/README.md
@@ -1,14 +1,14 @@
-# GelatoStock · prototipo 0.14.0
+# GelatoStock · prototipo 0.14.1
 
 Aplicación local de escritorio para gelatería, café de especialidad y postres. Esta entrega permite probar el circuito; no es aún la aplicación final de producción.
 
 ## Abrir en este Windows
 
-Hacé doble clic en **ABRIR GELATOSTOCK.vbs**, en esta carpeta. Abre una ventana propia, sin terminal. No requiere Node, Python, navegador ni conexión para usar el ejecutable ya generado. Si Windows bloquea VBScript, podés abrir `dist/GelatoStock-0.14.0-win32-x64/GelatoStock.exe` directamente; en ese caso los datos se guardan dentro de esa carpeta portátil.
+Hacé doble clic en **ABRIR GELATOSTOCK.vbs**, en esta carpeta. Abre una ventana propia, sin terminal. No requiere Node, Python, navegador ni conexión para usar el ejecutable ya generado. Si Windows bloquea VBScript, podés abrir `dist/GelatoStock-0.14.1-win32-x64/GelatoStock.exe` directamente; en ese caso los datos se guardan dentro de esa carpeta portátil.
 
 El acceso principal guarda datos, copias y perfil del programa en `D:/APPGELATOSTOCK/data`. Temporales de los procesos lanzados desde el acceso: `work/`. No se ha configurado almacenamiento del proyecto en C. Windows puede generar sus propios registros del sistema fuera del control de la app.
 
-No mover el ejecutable aislado: necesita el resto de archivos de su carpeta. Para copiarlo a otro Windows x64, copiar toda la carpeta `dist/GelatoStock-0.14.0-win32-x64` a una ubicación donde el usuario pueda escribir. Arranca con ejemplos nuevos, salvo que se restaure una copia. No sincroniza equipos.
+No mover el ejecutable aislado: necesita el resto de archivos de su carpeta. Para copiarlo a otro Windows x64, copiar toda la carpeta `dist/GelatoStock-0.14.1-win32-x64` a una ubicación donde el usuario pueda escribir. Arranca con ejemplos nuevos, salvo que se restaure una copia. No sincroniza equipos.
 
 ## Qué podés probar
 
@@ -23,7 +23,7 @@ No mover el ejecutable aislado: necesita el resto de archivos de su carpeta. Par
 9. Crear y restaurar copias en Configuración.
 10. Consultar el historial de movimientos y cerrar/reabrir sin perder datos.
 
-Todos los proveedores, precios y datos iniciales son ficticios. La aplicación no realiza pagos. Desde 0.14.0 puede enviar un pedido por WhatsApp solo si tú lo confirmas en pantalla, al número de la ficha del proveedor autorizado para la cuenta vinculada.
+Todos los proveedores, precios y datos iniciales son ficticios. La aplicación no realiza pagos. Desde 0.14.1 puede enviar un pedido por WhatsApp solo si tú lo confirmas en pantalla, al número de la ficha del proveedor autorizado para la cuenta vinculada.
 
 ## Qué está pendiente
 
@@ -115,46 +115,49 @@ Por defecto usa dos lecturas. Si discrepan, el documento queda por revisar. Cont
 
 Se eligió Q4 por mejores resultados medidos, no por aumentar el tamaño del modelo. El texto mostrado procede del original, nunca de explicaciones generadas. Sigue siendo un asistente local limitado, con errores y abstenciones posibles. Para Claude: docs/IA_LOCAL_Y_SEGURIDAD.md y último informe de reports/.
 
-## Auditoría y mejora continua en 0.14.0
+## Auditoría y mejora continua en 0.14.1
 La entrega 0.8.0 se auditó: parche y commit coinciden, pruebas y formato correctos. La comprobación de sumas acepta etiquetas habituales de facturas españolas (IVA 21%, Total a pagar) y sigue absteniéndose ante porcentajes sin importe o varias cuotas. El trabajador de IA bloquea más módulos de red; no es un sandbox del sistema. Cualquier IA que continúe debe leer docs/PROMPT_MAESTRO_V2.md y aplicar su directriz de mejora continua.
 
-## Chat de dudas en 0.14.0
+## Chat de dudas en 0.14.1
 IA local incluye un chat para preguntar cómo usar la app o qué dice el texto pegado en el editor. Responde el modelo local a partir de una guía fija y del texto del editor si se marca la casilla; no consulta el inventario ni los pedidos, no ejecuta acciones y sus respuestas son orientativas. Cada respuesta puede tardar de 30 segundos a más de un minuto en este equipo. El chat se conserva solo mientras la ventana está abierta.
 
-## Producción y respuestas de proveedores en 0.14.0
+## Producción y respuestas de proveedores en 0.14.1
 Producción: crea recetas (rinde X kg, ingredientes en su unidad base) y registra los kilos producidos por día. La app propone el consumo; corrígelo y aprueba para descontar ingredientes y sumar el producto terminado. La hoja diaria muestra kilos por gelato. Guía: docs/PRODUCCION_Y_RECETAS.md.
 
 Mensajes: cada respuesta de proveedor se lee con reglas (falta de producto, cancelación, cambio, pregunta, fecha de entrega, confirmación) y se marca si debes leerla; las fechas como «el lunes» se resuelven. En Resumen aparece cuántos mensajes debes leer y en Control de entregas las respuestas vinculadas a cada pedido. «Segunda lectura con IA local» pide al modelo una categoría como propuesta; no cambia pedidos ni stock.
 
-## Envío real por WhatsApp en 0.14.0
+## Envío real por WhatsApp en 0.14.1
 En Compras → Control de entregas, un pedido pendiente tiene «Enviar por WhatsApp». Se muestra el texto exacto y el destinatario; al confirmar se envía una sola vez desde la cuenta vinculada por QR. Requisitos: WhatsApp conectado y el número del proveedor autorizado en WhatsApp → Autorizar chat. Enviar no cambia stock ni confirma el pedido: la respuesta del proveedor llega a la pantalla WhatsApp y se lee por reglas. Prueba primero con un número propio.
 
-## Diagnóstico de WhatsApp en 0.14.0
+## Diagnóstico de WhatsApp en 0.14.1
 La pantalla WhatsApp muestra «Diagnóstico del canal» con los últimos eventos técnicos (remitente resuelto, autorizado o no, enviado, ignorado y por qué), sin contenido de mensajes, y un botón «Enviar mensaje de prueba» a un chat autorizado. Si un mensaje no aparece, ese diagnóstico dice el motivo.
 
-## WhatsApp comprobado en 0.14.0
+## WhatsApp comprobado en 0.14.1
 Envío y recepción reales verificados con dos números del propietario: la app envió un mensaje desde la cuenta vinculada y recibió la respuesta. Si un envío aparece como no confirmado, el diagnóstico del canal indica el motivo.
 
-## Mejora general en 0.14.0
+## Mejora general en 0.14.1
 Los mensajes de WhatsApp de proveedores autorizados aparecen también en Mensajes, con su lectura por reglas; si responden al pedido que les enviaste y es el único en curso, quedan vinculados y el pedido muestra la entrega prevista. La app hace una copia automática diaria (30 últimas en data/backups) y lo indica en Configuración. Puedes confirmar el tipo de documento de una foto desde IA local. El código de la interfaz está en src/ui/*.js.
 
-## Ventas y mermas en 0.14.0
+## Ventas y mermas en 0.14.1
 En Producción, la tabla «Ventas y mermas del día» registra kilos vendidos o desechados de cada producto terminado; cada cantidad crea una salida o merma en el historial, reversible como cualquier movimiento.
 
-## Conexión automática y avisos en 0.14.0
+## Conexión automática y avisos en 0.14.1
 En WhatsApp, «Conectar al abrir: sí» hace que la app reconecte sola al arrancar con la sesión guardada; cuando un proveedor autorizado escribe, Windows muestra una notificación y al pulsarla vuelve a la app. Configuración incluye «Primeros pasos».
 
-## Historial, reconexión y CSV en 0.14.0
+## Historial, reconexión y CSV en 0.14.1
 Al conectar WhatsApp, la app revisa los últimos mensajes de cada chat autorizado e importa los que llegaron con la app cerrada (texto). Si la sesión se cae y «Conectar al abrir» está activo, reintenta sola. Configuración → «Exportar CSV» crea inventario y movimientos en data/exportaciones para hoja de cálculo o gestoría.
 
-## Recetario protegido y limpieza en 0.14.0
+## Recetario protegido y limpieza en 0.14.1
 Configuración → «Recetario protegido»: pon una contraseña y la pantalla Producción pedirá desbloqueo (30 minutos); sin ella no se pueden crear recetas ni producir. Protege la pantalla dentro de la app, no cifra el disco; los movimientos de stock siguen visibles en Actividad. Configuración → «Limpieza periódica»: elige 7, 14, 30 o 90 días para borrar actividad y conversaciones antiguas (se conservan las 50 entradas recientes, los movimientos y las copias). Si olvidas la contraseña: con la app cerrada, borra la clave recipes_lock de la tabla settings en data/gelatostock.sqlite.
 
-## Aprendizaje por correcciones en 0.14.0
+## Aprendizaje por correcciones en 0.14.1
 En un mensaje, «Corregir lectura» te deja elegir qué quiso decir el proveedor (falta de producto, cancelación, cierre o vacaciones, pago, cambio, pregunta, documento, entrega, confirmación). La app lo recuerda y lo aplica a mensajes iguales o casi iguales; lo aprendido se ve y se olvida en Configuración. No es entrenamiento del modelo: son tus decisiones. Las reglas también entienden ahora horas, fechas con mes, abreviaturas y faltas de ortografía habituales.
 
-## Identidad visual en 0.14.0
+## Identidad visual en 0.14.1
 La app se presenta como propuesta para Artello (Palma de Mallorca) con una identidad propia: paleta «Mediterraneo» (pistacho, rosa y azafrán) sobre crema y acero, títulos en serif y una pantalla de inicio «arte + gelato». El nombre y el lugar se editan en Configuración → Identidad del negocio. No se incluyen logotipos ni materiales de la marca real.
 
-## Documentos por proveedor en 0.14.0
+## Documentos por proveedor en 0.14.1
 La pantalla Documentos reúne facturas, albaranes, recibos y pedidos (fotos o PDF) por proveedor. Para cada documento la app propone, por reglas, el proveedor, el tipo y el pedido al que pertenece (número de pedido en el texto, único pedido reciente del proveedor o importe coincidente); tú aceptas con un clic o vinculas a mano. Los adjuntos que envían por WhatsApp los proveedores autorizados entran solos. En Control de entregas se ven los documentos de cada pedido.
+
+## Sistema de diseño en 0.14.1
+Toda la interfaz sigue un sistema documentado en docs/DISENO_SISTEMA.md: paleta Mediterraneo con contraste AA, una sola escala de texto, ritmo de 8 px, componentes con estados y acento por pantalla. Se revisaron las once pantallas y los diálogos a cuatro tamaños sin solapamientos ni textos cortados.
diff --git a/TODO.md b/TODO.md
index 3e0b087..7fde0dd 100644
--- a/TODO.md
+++ b/TODO.md
@@ -1,6 +1,6 @@
 # TODO — estado del prototipo
 
-La especificación completa permanece en docs/TODO.md. Este archivo indica el estado real de 0.14.0.
+La especificación completa permanece en docs/TODO.md. Este archivo indica el estado real de 0.14.1.
 
 ## Entregado y comprobado
 
@@ -34,7 +34,9 @@ La especificación completa permanece en docs/TODO.md. Este archivo indica el es
 - [ ] Prueba de disco lleno y validación del corte en el equipo real.
 - [ ] Autenticación y permisos por persona si la app la usan varias personas (hoy: una contraseña opcional para el recetario).
 - [x] Identidad visual propia para la propuesta (paleta Mediterraneo, inicio arte + gelato, nombre y lugar editables).
-- [ ] Paleta exacta de Artello si la facilitan (hoy es una interpretación propia) y auditoría de contraste/teclado.
+- [x] Sistema de diseño con auditoría automática de contraste, tamaños, cortes y foco por teclado (work/design-capture.cjs, docs/DISENO_SISTEMA.md).
+- [ ] Paleta exacta de Artello si la facilitan (hoy es una interpretación propia).
+- [ ] Mensajes: conversación por proveedor, respuestas rápidas por WhatsApp y «qué hacer ahora»; pantalla «Guía» dentro de la app (docs/GUIA_USO.md ya escrita).
 - [ ] Empaquetar, firmar y probar en el Mac real del usuario (modelo y RAM por confirmar).
 - [ ] Firmar la distribución Windows y crear instaladores/actualizaciones seguras.
 
diff --git a/package.json b/package.json
index 582ac64..f5c0966 100644
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "appgelatostock",
-  "version": "0.14.0",
+  "version": "0.14.1",
   "private": true,
   "description": "Prototipo local de inventario y compras para gelateria",
   "main": "src/desktop.cjs",
diff --git a/src/styles.css b/src/styles.css
index 348c726..5ddc40d 100644
--- a/src/styles.css
+++ b/src/styles.css
@@ -1,34 +1,120 @@
+/* GelatoStock · sistema de diseño «Mediterraneo»
+   Tokens en :root, escala tipográfica 12/13/14/15/18/22/32, ritmo de 8 px.
+   Documentación: docs/DISENO_SISTEMA.md. Sin fuentes web: la CSP exige 'self'. */
+
+:root {
+  /* Paleta base */
+  --pistacho: #35604a;
+  --pistacho-oscuro: #2f5a44;
+  --pistacho-claro: #e6eedc;
+  --pistacho-suave: #f0f4e9;
+  --rosa: #c85c73;
+  --rosa-texto: #9a3652;
+  --rosa-claro: #fbe7ec;
+  --azafran: #e0a53a;
+  --azafran-texto: #7f5410;
+  --azafran-claro: #fbeccf;
+  --lavanda-texto: #5d4e8c;
+  --lavanda-claro: #eee9f4;
+  --arena-claro: #f3eddf;
+  --crema: #fbf7f0;
+  --crema-panel: #fffdf9;
+  --crema-hundida: #f5f0e6;
+  --acero: #6b7680;
+  --tinta: #1e2a2f;
+  /* Texto: todos ≥ 4,5:1 sobre crema, panel y tintes claros */
+  --ink: var(--tinta);
+  --ink-soft: #4a5560;
+  --ink-muted: #5a6670;
+  --ink-title: var(--pistacho);
+  /* Superficies y bordes */
+  --bg: var(--crema);
+  --surface: var(--crema-panel);
+  --surface-2: var(--crema-hundida);
+  --border: #e6dfd2;
+  --border-soft: #efeae0;
+  --line: #ece7dc;
+  /* Acento por pantalla (se redefine en main.page-…) */
+  --accent: var(--pistacho);
+  --accent-soft: var(--pistacho-claro);
+  --accent-text: var(--pistacho);
+  /* Interacción */
+  --focus: #8fb07a;
+  --primary: var(--pistacho);
+  --primary-hover: var(--pistacho-oscuro);
+  --danger: var(--rosa-texto);
+  /* Tipografía */
+  --font: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
+  --font-serif: Georgia, "Times New Roman", serif;
+  --fs-12: 12px;
+  --fs-13: 13px;
+  --fs-14: 14px;
+  --fs-15: 15px;
+  --fs-18: 18px;
+  --fs-22: 22px;
+  --fs-32: 32px;
+  --lh-title: 1.2;
+  --lh-text: 1.55;
+  --measure: 64ch;
+  /* Ritmo de 8 px */
+  --s-1: 4px;
+  --s-2: 8px;
+  --s-3: 12px;
+  --s-4: 16px;
+  --s-5: 24px;
+  --s-6: 32px;
+  --s-7: 40px;
+  --s-8: 48px;
+  /* Radios y sombras */
+  --r-sm: 6px;
+  --r-md: 10px;
+  --r-lg: 14px;
+  --r-xl: 18px;
+  --shadow-sm: 0 1px 2px rgba(30, 42, 47, 0.06);
+  --shadow-md: 0 8px 24px rgba(30, 42, 47, 0.1);
+  --shadow-lg: 0 24px 80px rgba(30, 42, 47, 0.18);
+  /* Medidas de estructura */
+  --sidebar: 240px;
+  --topbar: 64px;
+  --control-h: 40px;
+  --control-sm: 32px;
+  --content-max: 1560px;
+  --content-pad: 32px;
+}
+
+/* ---------- Base ---------- */
 * {
   box-sizing: border-box;
 }
+html {
+  height: 100%;
+}
 body {
   margin: 0;
-  background: #faf6ee;
-  color: #2e3430;
-  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
-  font-size: 14px;
-  line-height: 1.5;
+  background: var(--bg);
+  color: var(--ink);
+  font-family: var(--font);
+  font-size: var(--fs-14);
+  line-height: var(--lh-text);
+  -webkit-font-smoothing: antialiased;
 }
 button,
 input,
 select,
 textarea {
   font: inherit;
+  color: inherit;
 }
 button {
   cursor: pointer;
 }
 button:disabled {
-  opacity: 0.45;
+  opacity: 0.5;
   cursor: not-allowed;
 }
-button:focus-visible,
-a:focus-visible,
-input:focus-visible,
-select:focus-visible,
-textarea:focus-visible {
-  outline: 3px solid #8fb07a;
-  outline-offset: 3px;
+:focus-visible {
+  outline: 3px solid var(--focus);
+  outline-offset: 2px;
 }
 svg {
   flex-shrink: 0;
@@ -41,580 +127,836 @@ a {
 h1,
 h2,
 h3,
-p {
+p,
+figure {
   margin: 0;
 }
 h1 {
-  font-family: Georgia, "Times New Roman", serif;
-  font-size: 32px;
-  letter-spacing: -0.6px;
-  line-height: 1.2;
+  font-family: var(--font-serif);
+  font-size: var(--fs-32);
   font-weight: 500;
-  color: #35604a;
+  line-height: var(--lh-title);
+  letter-spacing: -0.4px;
+  color: var(--ink-title);
 }
 h2 {
-  font-size: 17px;
+  font-size: var(--fs-18);
   font-weight: 650;
-  letter-spacing: -0.35px;
+  line-height: var(--lh-title);
+  letter-spacing: -0.2px;
+  color: var(--ink);
 }
 h3 {
-  font-size: 15px;
+  font-size: var(--fs-15);
   font-weight: 650;
+  line-height: var(--lh-title);
+  color: var(--ink);
+}
+p {
+  max-width: var(--measure);
 }
 small {
-  font-size: 12px;
+  font-size: var(--fs-12);
+  line-height: 1.5;
+  color: var(--ink-muted);
 }
 strong {
-  font-weight: 620;
+  font-weight: 650;
+}
+code {
+  font-family: Consolas, "Cascadia Mono", Menlo, monospace;
+  font-size: var(--fs-12);
+}
+ul,
+ol {
+  margin: 0;
+}
+.muted {
+  font-size: var(--fs-13);
+  color: var(--ink-muted);
+}
+.fineprint {
+  font-size: var(--fs-12);
+  color: var(--ink-muted);
+  line-height: 1.55;
+  margin-top: var(--s-3);
+  max-width: var(--measure);
+}
+.sr-only {
+  position: absolute;
+  width: 1px;
+  height: 1px;
+  padding: 0;
+  overflow: hidden;
+  clip: rect(0, 0, 0, 0);
+  white-space: nowrap;
+  border: 0;
+}
+.boot {
+  margin: auto;
+  color: var(--ink-muted);
+}
+.dot {
+  display: inline-block;
+  width: 7px;
+  height: 7px;
+  background: var(--accent);
+  border-radius: 50%;
+  flex: none;
+}
+[role="alert"]:not(:empty) {
+  color: var(--danger);
+  font-size: var(--fs-13);
+  margin-top: var(--s-2);
 }
+
+/* ---------- Estructura ---------- */
 #app {
   display: flex;
   min-height: 100vh;
 }
+main {
+  margin-left: var(--sidebar);
+  min-width: 0;
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+}
+.content {
+  padding: var(--content-pad) var(--content-pad) var(--s-5);
+  max-width: var(--content-max);
+  width: 100%;
+  margin: 0 auto;
+  flex: 1;
+}
+.content > * + * {
+  margin-top: var(--s-5);
+}
+footer {
+  display: flex;
+  justify-content: space-between;
+  gap: var(--s-4);
+  flex-wrap: wrap;
+  font-size: var(--fs-12);
+  color: var(--ink-muted);
+  padding: var(--s-4) var(--content-pad) var(--s-5);
+  max-width: var(--content-max);
+  width: 100%;
+  margin: 0 auto;
+}
+
+/* Acento por pantalla: identidad sin ruido */
+main.page-home {
+  --accent: var(--pistacho);
+  --accent-soft: var(--pistacho-claro);
+  --accent-text: var(--pistacho);
+}
+main.page-stock {
+  --accent: #5c8a4c;
+  --accent-soft: var(--pistacho-claro);
+  --accent-text: #2f5a44;
+}
+main.page-orders {
+  --accent: var(--azafran);
+  --accent-soft: var(--azafran-claro);
+  --accent-text: var(--azafran-texto);
+}
+main.page-production {
+  --accent: #7f9b5a;
+  --accent-soft: var(--pistacho-suave);
+  --accent-text: #3f6a2f;
+}
+main.page-messages {
+  --accent: var(--rosa);
+  --accent-soft: var(--rosa-claro);
+  --accent-text: var(--rosa-texto);
+}
+main.page-whatsapp {
+  --accent: #4a7a5c;
+  --accent-soft: var(--pistacho-claro);
+  --accent-text: var(--pistacho);
+}
+main.page-suppliers {
+  --accent: #c99a42;
+  --accent-soft: var(--azafran-claro);
+  --accent-text: var(--azafran-texto);
+}
+main.page-documents {
+  --accent: var(--acero);
+  --accent-soft: var(--arena-claro);
+  --accent-text: #4a5560;
+}
+main.page-activity {
+  --accent: var(--acero);
+  --accent-soft: var(--crema-hundida);
+  --accent-text: #4a5560;
+}
+main.page-ai {
+  --accent: #8a78b8;
+  --accent-soft: var(--lavanda-claro);
+  --accent-text: var(--lavanda-texto);
+}
+main.page-settings {
+  --accent: var(--acero);
+  --accent-soft: var(--crema-hundida);
+  --accent-text: #4a5560;
+}
+
+/* ---------- Barra lateral ---------- */
 .sidebar {
-  width: 238px;
+  width: var(--sidebar);
   flex: none;
   position: fixed;
   inset: 0 auto 0 0;
-  background: #fffdf9;
-  border-right: 1px solid #ebe4d8;
-  padding: 31px 19px 15px;
+  background: var(--surface);
+  border-right: 1px solid var(--border-soft);
+  padding: var(--s-5) var(--s-4) var(--s-4);
   display: flex;
   flex-direction: column;
   z-index: 3;
+  overflow-y: auto;
+  scrollbar-width: thin;
+  scrollbar-color: var(--border) transparent;
 }
 .brand {
   display: flex;
-  gap: 10px;
+  flex-direction: column;
+  gap: var(--s-2);
+  padding: 0 var(--s-2);
+}
+.brand-row {
+  display: flex;
   align-items: center;
-  font-size: 23px;
-  letter-spacing: -1.2px;
+  gap: var(--s-3);
+  font-size: var(--fs-22);
+  letter-spacing: -0.8px;
   font-weight: 750;
-  padding: 0 7px;
+  color: var(--ink);
+  line-height: 1;
 }
 .brandmark {
-  width: 36px;
+  width: 40px;
   height: 40px;
-  background: #e6eedc;
-  border-radius: 13px;
+  background: var(--pistacho-claro);
+  border-radius: 12px;
   display: grid;
   place-items: center;
-  color: #35604a;
+  color: var(--pistacho);
   transform: rotate(-5deg);
+  flex: none;
 }
 .brandmark svg {
-  width: 25px;
-  height: 25px;
+  width: 24px;
+  height: 24px;
 }
 .brand-light {
   font-weight: 350;
 }
 .brand small {
   display: block;
-  font-size: 8px;
-  letter-spacing: 1.8px;
-  font-weight: 600;
-  margin-top: 1px;
-  color: #8d9490;
+  font-size: 11px;
+  letter-spacing: 1.6px;
+  font-weight: 650;
+  color: var(--ink-muted);
+  white-space: nowrap;
 }
 .workspace {
   display: flex;
-  gap: 10px;
+  gap: var(--s-3);
   align-items: center;
-  padding: 16px 9px;
-  margin: 29px 0 24px;
-  border-top: 1px solid #efe9de;
-  border-bottom: 1px solid #efe9de;
+  padding: var(--s-3) var(--s-2);
+  margin: var(--s-4) 0;
+  border-top: 1px solid var(--border-soft);
+  border-bottom: 1px solid var(--border-soft);
+  min-width: 0;
+}
+.workspace > div {
+  min-width: 0;
 }
 .workspace strong {
-  font-size: 13px;
+  font-size: var(--fs-14);
+  display: block;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
 .workspace small {
-  font-size: 10px;
-  color: #8c9286;
   display: block;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
 .workspace-icon {
-  height: 33px;
-  width: 33px;
+  height: 36px;
+  width: 36px;
+  flex: none;
   display: grid;
   place-items: center;
-  background: #f0ecdf;
-  border: 1px solid #e9e3d5;
-  border-radius: 10px;
-  font-family: Georgia, serif;
+  background: var(--pistacho);
+  color: var(--crema-panel);
+  border-radius: var(--r-md);
+  font-family: var(--font-serif);
   font-size: 20px;
-  color: #736b56;
+  font-weight: 500;
 }
 .nav-label {
-  font-size: 9px;
+  font-size: 11px;
   letter-spacing: 1.6px;
-  color: #a1a598;
-  padding: 0 13px;
-  margin-bottom: 10px;
+  font-weight: 650;
+  color: var(--ink-muted);
+  padding: 0 var(--s-3);
+  margin-bottom: var(--s-2);
+}
+nav {
+  display: flex;
+  flex-direction: column;
+  gap: 2px;
 }
 .nav-item {
   width: 100%;
   display: flex;
   align-items: center;
-  gap: 13px;
+  gap: var(--s-3);
   border: 0;
   background: transparent;
   text-align: left;
-  padding: 12px 13px;
-  margin: 3px 0;
-  color: #7b8379;
-  border-radius: 9px;
-  font-size: 13px;
+  padding: 9px var(--s-3);
+  min-height: 38px;
+  color: var(--ink-soft);
+  border-radius: var(--r-md);
+  font-size: var(--fs-14);
+  line-height: 1.3;
+  position: relative;
 }
 .nav-item svg {
   width: 18px;
   height: 18px;
+  color: var(--ink-muted);
+}
+.nav-item:hover {
+  background: var(--crema-hundida);
+  color: var(--ink);
 }
 .nav-item.active {
-  background: #e6eedc;
-  color: #35604a;
+  background: var(--pistacho-claro);
+  color: var(--pistacho);
   font-weight: 650;
+  box-shadow: inset 3px 0 0 var(--rosa);
 }
-.nav-item:hover {
-  background: #f2efe6;
+.nav-item.active svg {
+  color: var(--pistacho);
 }
 .nav-count {
-  font-size: 10px;
-  background: #e6eedc;
-  color: #35604a;
-  border-radius: 6px;
-  padding: 1px 6px;
+  font-size: var(--fs-12);
+  font-weight: 650;
+  background: var(--pistacho);
+  color: var(--crema-panel);
+  border-radius: 999px;
+  padding: 1px 8px;
   margin-left: auto;
+  line-height: 1.5;
+}
+.nav-item.active .nav-count {
+  background: var(--rosa-texto);
 }
 .sidebar-bottom {
   margin-top: auto;
-  padding-top: 30px;
+  padding-top: var(--s-4);
 }
 .local-card {
-  background: #f4f5ee;
-  border: 1px solid #ebe4d8;
-  border-radius: 12px;
-  padding: 16px 13px;
-  margin: 0 3px 16px;
+  background: var(--pistacho-suave);
+  border: 1px solid var(--pistacho-claro);
+  border-radius: var(--r-md);
+  padding: var(--s-3) var(--s-3);
+  margin-bottom: var(--s-3);
 }
 .local-card > svg {
-  color: #779071;
-  margin-bottom: 8px;
+  color: var(--pistacho);
+  margin-bottom: var(--s-1);
+  width: 18px;
+  height: 18px;
 }
 .local-card strong {
   display: block;
-  font-size: 11px;
+  font-size: var(--fs-13);
+  color: var(--ink);
 }
 .local-card p {
-  font-size: 10px;
-  color: #8a9183;
-  line-height: 1.7;
-  margin: 5px 0 12px;
+  font-size: var(--fs-12);
+  color: var(--ink-muted);
+  line-height: 1.5;
+  margin: var(--s-1) 0 var(--s-2);
 }
 .local-card span {
-  font-size: 10px;
-  color: #658061;
+  font-size: var(--fs-12);
+  color: var(--pistacho);
   display: flex;
-  gap: 6px;
+  gap: var(--s-2);
   align-items: center;
 }
-.dot {
-  display: inline-block;
-  width: 6px;
-  height: 6px;
-  background: #79a57d;
-  border-radius: 50%;
+.local-card .dot {
+  background: var(--pistacho);
 }
 .profile {
   display: flex;
-  gap: 10px;
+  gap: var(--s-3);
   align-items: center;
-  border-top: 1px solid #efe9de;
-  margin-top: 14px;
-  padding: 16px 7px 0;
+  border-top: 1px solid var(--border-soft);
+  margin-top: var(--s-2);
+  padding: var(--s-3) var(--s-2) 0;
+  min-width: 0;
+}
+.profile > div {
+  min-width: 0;
 }
 .profile strong {
-  font-size: 12px;
+  font-size: var(--fs-13);
+  display: block;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
 .profile small {
   display: block;
-  color: #93988e;
-  font-size: 10px;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
 .avatar {
   display: grid;
   place-items: center;
-  width: 35px;
-  height: 35px;
+  width: 36px;
+  height: 36px;
+  flex: none;
   border-radius: 50%;
-  background: #eee8dc;
-  color: #817562;
-  font-size: 10px;
+  background: var(--arena-claro);
+  color: var(--azafran-texto);
+  font-size: var(--fs-12);
   font-weight: 650;
 }
 .avatar.small {
-  width: 31px;
-  height: 31px;
-}
-main {
-  margin-left: 238px;
-  min-width: 0;
-  flex: 1;
+  width: 32px;
+  height: 32px;
 }
+
+/* ---------- Barra superior ---------- */
 .topbar {
-  height: 76px;
-  border-bottom: 1px solid #e9ebe2;
+  height: var(--topbar);
+  border-bottom: 1px solid var(--border-soft);
   display: flex;
   align-items: center;
   justify-content: space-between;
-  padding: 0 38px;
-  background: #fffdf9;
+  gap: var(--s-4);
+  padding: 0 var(--content-pad);
+  background: var(--surface);
+  position: sticky;
+  top: 0;
+  z-index: 2;
 }
 .breadcrumb {
-  font-size: 11px;
-  color: #7a8376;
+  font-size: var(--fs-13);
+  color: var(--ink-muted);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
 .breadcrumb span {
-  margin: 0 12px;
-  color: #c1c7b8;
+  margin: 0 var(--s-2);
+  color: var(--ink-muted);
 }
 .top-right {
   display: flex;
   align-items: center;
-  gap: 20px;
+  gap: var(--s-3);
+  flex: none;
 }
 .local-status {
-  font-size: 10px;
+  font-size: var(--fs-12);
   display: flex;
-  gap: 7px;
+  gap: var(--s-2);
   align-items: center;
-  color: #71816b;
+  color: var(--pistacho);
+  background: var(--pistacho-suave);
+  border-radius: 999px;
+  padding: 4px 10px 4px 8px;
+  white-space: nowrap;
+}
+.local-status .dot {
+  background: var(--pistacho);
 }
 .icon-button {
   border: 0;
   background: transparent;
-  color: #7c8476;
-  width: 32px;
-  height: 32px;
+  color: var(--ink-soft);
+  width: 36px;
+  height: 36px;
   display: inline-flex;
   align-items: center;
   justify-content: center;
   position: relative;
-  border-radius: 7px;
+  border-radius: var(--r-sm);
+  flex: none;
 }
 .icon-button:hover {
-  background: #ebeee4;
+  background: var(--crema-hundida);
+  color: var(--ink);
+}
+.icon-button.bordered {
+  border: 1px solid var(--border);
+  background: var(--surface);
+  width: 32px;
+  height: 32px;
+}
+.icon-button.bordered svg {
+  width: 16px;
+  height: 16px;
 }
 .notification-dot {
   position: absolute;
-  right: 6px;
-  top: 5px;
-  width: 5px;
-  height: 5px;
-  background: #aa715c;
-  border: 1px solid #fffdf9;
+  right: 8px;
+  top: 7px;
+  width: 7px;
+  height: 7px;
+  background: var(--rosa);
+  border: 1px solid var(--surface);
   border-radius: 50%;
 }
-.content {
-  padding: 32px 38px 24px;
-  max-width: 1640px;
-  margin: auto;
-}
+
+/* ---------- Cabecera de página ---------- */
 .page-heading {
   display: flex;
-  align-items: center;
+  align-items: flex-end;
   justify-content: space-between;
-  gap: 20px;
-  margin-bottom: 26px;
+  gap: var(--s-4) var(--s-5);
+  flex-wrap: wrap;
+}
+.page-heading > div:first-child {
+  min-width: 0;
+  flex: 1 1 420px;
 }
 .eyebrow {
-  font-size: 8px;
-  letter-spacing: 2px;
-  color: #919986;
-  margin-bottom: 9px;
+  font-size: var(--fs-12);
+  letter-spacing: 1.8px;
   font-weight: 650;
+  color: var(--accent-text);
+  margin-bottom: var(--s-2);
+  display: flex;
+  align-items: center;
+  gap: var(--s-2);
 }
 .page-heading p {
-  color: #909787;
-  margin-top: 9px;
-  font-size: 12px;
+  color: var(--ink-soft);
+  margin-top: var(--s-2);
+  font-size: var(--fs-14);
 }
 .heading-actions {
   display: flex;
-  gap: 9px;
-  flex-shrink: 0;
+  gap: var(--s-2);
+  flex-wrap: wrap;
+  flex: none;
+  justify-content: flex-end;
 }
+
+/* ---------- Botones ---------- */
 .btn {
-  border: 1px solid #dfe3d7;
-  padding: 10px 15px;
-  border-radius: 8px;
-  background: white;
+  border: 1px solid var(--border);
+  padding: 0 var(--s-4);
+  border-radius: var(--r-md);
+  background: var(--surface);
   display: inline-flex;
-  gap: 8px;
+  gap: var(--s-2);
   align-items: center;
   justify-content: center;
-  color: #596451;
-  font-size: 11px;
+  color: var(--ink-soft);
+  font-size: var(--fs-13);
   font-weight: 600;
-  min-height: 38px;
+  min-height: var(--control-h);
+  line-height: 1.2;
+  white-space: nowrap;
+  text-align: center;
+  box-shadow: var(--shadow-sm);
+  transition:
+    background 0.15s,
+    border-color 0.15s,
+    color 0.15s;
 }
 .btn svg {
   width: 16px;
   height: 16px;
 }
+.btn:hover {
+  background: var(--crema-hundida);
+  border-color: #d9d1c2;
+  color: var(--ink);
+}
+.btn:active {
+  transform: translateY(1px);
+}
+.btn:disabled:hover {
+  background: var(--surface);
+  color: var(--ink-soft);
+}
 .btn.primary {
-  background: #35604a;
-  border-color: #35604a;
+  background: var(--primary);
+  border-color: var(--primary);
   color: #fff;
+  box-shadow: 0 6px 14px rgba(53, 96, 74, 0.2);
 }
 .btn.primary:hover {
-  background: #27493a;
+  background: var(--primary-hover);
+  border-color: var(--primary-hover);
+  color: #fff;
 }
-.btn.secondary:hover {
-  background: #f2efe6;
+.btn.primary:disabled:hover {
+  background: var(--primary);
+}
+.btn.danger {
+  color: var(--danger);
+  border-color: #efc9d2;
+  background: var(--surface);
+}
+.btn.danger:hover {
+  background: var(--rosa-claro);
+  color: var(--danger);
 }
 .btn.cream {
-  background: #f6efe2;
-  color: #4c4f3d;
-  border-color: #f6efe2;
-  margin-top: 19px;
+  background: var(--crema-panel);
+  color: var(--pistacho);
+  border-color: var(--crema-panel);
+  box-shadow: var(--shadow-md);
+}
+.btn.cream:hover {
+  background: #fff;
+  color: var(--pistacho-oscuro);
 }
 .btn.full {
   width: 100%;
 }
-.hero {
-  background: linear-gradient(135deg, #35604a 0%, #4a7a5c 55%, #7f9b5a 100%);
-  border-radius: 15px;
-  min-height: 257px;
-  display: flex;
-  position: relative;
-  overflow: hidden;
-  color: #fffdf9;
+.text-button {
+  display: inline-flex;
+  align-items: center;
+  gap: var(--s-1);
+  border: 0;
+  background: transparent;
+  color: var(--pistacho);
+  font-size: var(--fs-13);
+  font-weight: 600;
+  white-space: nowrap;
+  padding: 4px 6px;
+  border-radius: var(--r-sm);
+  min-height: 28px;
 }
-.hero-copy {
-  padding: 28px 32px;
-  z-index: 1;
-  position: relative;
+.text-button svg {
+  width: 14px;
+  height: 14px;
 }
-.hero-label {
-  font-size: 8px;
-  letter-spacing: 1.8px;
-  color: #d8e6c8;
+.text-button:hover {
+  background: var(--pistacho-suave);
+  color: var(--pistacho-oscuro);
+}
+.row-actions,
+.setting-actions,
+.message-actions {
   display: flex;
-  gap: 7px;
+  gap: var(--s-2);
+  flex-wrap: wrap;
   align-items: center;
 }
-.hero-label .dot {
-  background: #e0a53a;
+.setting-actions {
+  margin-top: var(--s-4);
 }
-.hero h2 {
-  font-family: Georgia, "Times New Roman", serif;
-  font-weight: 400;
-  font-size: 35px;
-  line-height: 1.16;
-  letter-spacing: -0.8px;
-  margin-top: 15px;
+.setting-actions > small {
+  flex-basis: 100%;
 }
-.hero p {
-  font-size: 11px;
-  line-height: 1.75;
-  color: #d8e6c8;
-  margin-top: 13px;
+
+/* ---------- Píldoras y avatares ---------- */
+.pill {
+  font-size: var(--fs-12);
+  font-weight: 600;
+  padding: 3px 9px;
+  border-radius: 999px;
+  display: inline-flex;
+  align-items: center;
+  gap: 4px;
+  white-space: nowrap;
+  line-height: 1.5;
+  background: var(--crema-hundida);
+  color: var(--ink-soft);
 }
-.hero-art {
-  position: absolute;
-  right: 35px;
-  top: 0;
-  width: 380px;
-  height: 100%;
+.pill.neutral {
+  background: var(--crema-hundida);
+  color: var(--ink-soft);
 }
-.art-orbit {
-  position: absolute;
-  width: 300px;
-  height: 300px;
-  border-radius: 50%;
-  border: 1px solid #5c7f63;
-  right: 4px;
-  top: 18px;
+.sage {
+  background: var(--pistacho-claro);
+  color: var(--pistacho-oscuro);
 }
-.art-orbit:after {
-  content: "";
-  position: absolute;
-  inset: 17px;
-  border-radius: 50%;
-  border: 1px solid #596b51;
+.peach {
+  background: var(--rosa-claro);
+  color: var(--rosa-texto);
 }
-.scoop {
-  position: absolute;
-  width: 107px;
-  height: 101px;
-  border-radius: 49% 51% 42% 44%;
-  box-shadow:
-    inset -10px -10px 22px #00000012,
-    inset 6px 5px 15px #ffffff22;
+.lavender {
+  background: var(--lavanda-claro);
+  color: var(--lavanda-texto);
 }
-.scoop-one {
-  background: #b9cf98;
-  left: 95px;
-  top: 52px;
-  transform: rotate(-14deg);
+.sand {
+  background: var(--azafran-claro);
+  color: var(--azafran-texto);
 }
-.scoop-two {
-  background: #efb7c3;
-  left: 175px;
-  top: 54px;
-  transform: rotate(14deg);
+.rose {
+  background: #f6e3de;
+  color: #8c4a3a;
 }
-.scoop-three {
-  background: #f1cf7e;
-  left: 132px;
-  top: 105px;
-  z-index: 1;
+.coral {
+  background: var(--rosa-claro);
+  color: var(--rosa-texto);
 }
-.gelato-cup {
-  position: absolute;
-  top: 155px;
-  left: 91px;
-  width: 190px;
-  height: 100px;
-  background: linear-gradient(100deg, #eceadb, #fffdf9 45%, #e6e4d6);
-  clip-path: polygon(0 0, 100% 0, 84% 100%, 17% 100%);
-  border-radius: 7px 7px 35px 35px;
-  z-index: 2;
-  display: flex;
+.stat-icon {
+  width: 40px;
+  height: 40px;
+  display: inline-flex;
+  align-items: center;
   justify-content: center;
-  color: #5a6a4e;
-  font-family: Georgia, serif;
-  font-style: italic;
-  font-size: 62px;
-  box-shadow: inset 0 6px 0 #e3e2d3;
-}
-.art-caption {
-  position: absolute;
-  right: -5px;
-  top: 61px;
-  font-family: Georgia, serif;
-  font-style: italic;
-  font-size: 13px;
-  color: #c1cba9;
-  transform: rotate(8deg);
-}
-.art-dot {
-  position: absolute;
-  width: 10px;
-  height: 10px;
-  background: #c0c998;
-  border-radius: 50%;
-  left: 50px;
-  top: 99px;
-}
-.stats {
-  display: grid;
-  grid-template-columns: repeat(4, minmax(0, 1fr));
-  gap: 14px;
-  margin: 22px 0;
+  border-radius: var(--r-md);
+  flex-shrink: 0;
 }
-.stat {
-  background: #fffdf9;
-  border: 1px solid #e8eade;
-  border-radius: 12px;
-  padding: 20px 17px;
-  display: flex;
-  gap: 13px;
-  min-width: 0;
+.stat-icon svg {
+  width: 20px;
+  height: 20px;
 }
-.stat-icon {
-  width: 38px;
-  height: 38px;
+.supplier-avatar {
   display: inline-flex;
+  width: 36px;
+  height: 36px;
   align-items: center;
   justify-content: center;
-  border-radius: 11px;
+  border-radius: var(--r-md);
+  font-size: var(--fs-12);
+  font-weight: 650;
   flex-shrink: 0;
 }
-.sage {
-  background: #eaf1df;
-  color: #6f8a5a;
+.supplier-avatar.large {
+  width: 48px;
+  height: 48px;
+  font-size: var(--fs-15);
+  border-radius: var(--r-lg);
+  margin-bottom: var(--s-4);
 }
-.peach {
-  background: #fbe7ec;
-  color: #b04a62;
+.unread-dot {
+  width: 8px;
+  height: 8px;
+  border-radius: 50%;
+  background: var(--rosa);
+  flex-shrink: 0;
+  margin-left: auto;
 }
-.lavender {
-  background: #eee9f4;
-  color: #9785aa;
+.count-bubble {
+  font-size: var(--fs-12);
+  font-weight: 650;
+  border-radius: 50%;
+  background: var(--rosa-claro);
+  color: var(--rosa-texto);
+  width: 24px;
+  height: 24px;
+  display: grid;
+  place-items: center;
+  flex: none;
 }
-.sand {
-  background: #f3eddf;
-  color: #a18a5f;
+
+/* ---------- Avisos ---------- */
+.notice {
+  display: flex;
+  align-items: flex-start;
+  gap: var(--s-3);
+  background: var(--arena-claro);
+  border: 1px solid #ebe1cc;
+  border-radius: var(--r-md);
+  padding: var(--s-4) var(--s-4);
+  color: var(--ink-soft);
+}
+.notice > svg {
+  width: 20px;
+  height: 20px;
+  color: var(--accent-text);
+  margin-top: 2px;
 }
-.rose {
-  background: #f2e5e1;
-  color: #ad8377;
+.notice > div {
+  min-width: 0;
+  flex: 1;
 }
-.stat p {
-  color: #929a89;
-  font-size: 10px;
-  margin-bottom: 8px;
+.notice strong {
+  font-size: var(--fs-14);
+  display: block;
+  color: var(--ink);
 }
-.stat strong {
+.notice span {
+  font-size: var(--fs-13);
   display: block;
-  font-size: 28px;
-  font-weight: 550;
-  line-height: 1.1;
-  letter-spacing: -1px;
+  margin-top: 2px;
+  max-width: var(--measure);
 }
-.stat .money-value {
-  font-size: 22px;
-  line-height: 1.4;
-  white-space: nowrap;
+.notice.subtle {
+  background: var(--pistacho-suave);
+  border-color: var(--pistacho-claro);
 }
-.stat small {
-  display: block;
-  color: #a0a694;
-  font-size: 9px;
-  margin-top: 9px;
+.notice.inline {
+  margin: var(--s-4) 0 0;
+  font-size: var(--fs-13);
+  align-items: center;
+  padding: var(--s-3) var(--s-4);
 }
-.dashboard-grid {
+.notice.inline > svg {
+  margin: 0;
+}
+.home-notice > div {
   display: grid;
-  grid-template-columns: minmax(0, 1.85fr) minmax(270px, 1fr);
-  gap: 20px;
+  gap: 2px;
+}
+.home-notice strong {
+  margin-top: var(--s-3);
 }
+.home-notice strong:first-child {
+  margin-top: 0;
+}
+.home-notice .text-button {
+  justify-self: start;
+  margin-left: -6px;
+}
+
+/* ---------- Paneles y tablas ---------- */
 .panel {
-  background: #fffdf9;
-  border: 1px solid #ebe4d8;
-  border-radius: 12px;
+  background: var(--surface);
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
   overflow: hidden;
   min-width: 0;
+  box-shadow: var(--shadow-sm);
 }
 .panel-heading {
   display: flex;
-  align-items: center;
+  align-items: flex-start;
   justify-content: space-between;
-  padding: 23px 23px 18px;
-  gap: 10px;
+  gap: var(--s-3) var(--s-4);
+  padding: var(--s-5) var(--s-5) var(--s-4);
+  flex-wrap: wrap;
 }
-.panel-heading p {
-  color: #8d9490;
-  font-size: 10px;
-  margin-top: 4px;
+.panel-heading > div {
+  min-width: 0;
+  flex: 1 1 280px;
 }
-.text-button {
-  display: inline-flex;
-  align-items: center;
-  gap: 6px;
-  border: 0;
-  background: transparent;
-  color: #64805b;
-  font-size: 10px;
-  white-space: nowrap;
+.panel-heading p {
+  color: var(--ink-muted);
+  font-size: var(--fs-13);
+  margin-top: var(--s-1);
 }
-.text-button svg {
-  width: 13px;
-  height: 13px;
+.panel-heading .text-button,
+.panel-heading .pill,
+.panel-heading .count-bubble {
+  flex: none;
+  align-self: center;
 }
-.table-wrap {
+.table-wrap,
+.table-scroll {
   overflow: auto;
 }
 table {
@@ -624,36 +966,45 @@ table {
   white-space: nowrap;
 }
 th {
-  font-size: 9px;
-  color: #969f8b;
-  background: #fafbf5;
-  font-weight: 500;
-  padding: 12px 20px;
-  border-top: 1px solid #f2efe6;
-  border-bottom: 1px solid #f2efe6;
+  font-size: var(--fs-12);
+  color: var(--ink-muted);
+  background: var(--crema-hundida);
+  font-weight: 600;
+  letter-spacing: 0.2px;
+  padding: var(--s-3) var(--s-5);
+  border-top: 1px solid var(--border-soft);
+  border-bottom: 1px solid var(--border-soft);
 }
 td {
-  padding: 17px 20px;
-  border-bottom: 1px solid #eff1e8;
-  font-size: 11px;
-  color: #79816f;
+  padding: var(--s-4) var(--s-5);
+  border-bottom: 1px solid var(--border-soft);
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
+  vertical-align: middle;
 }
 tr:last-child td {
   border-bottom: 0;
 }
 td strong {
-  color: #46503f;
-  font-size: 12px;
+  color: var(--ink);
+  font-size: var(--fs-14);
+}
+td small {
+  display: block;
+  margin-top: 2px;
 }
 .product-cell {
   display: flex;
-  gap: 12px;
+  gap: var(--s-3);
   align-items: center;
 }
+.product-cell > div {
+  min-width: 0;
+}
 .product-icon {
-  width: 34px;
-  height: 38px;
-  border-radius: 9px;
+  width: 36px;
+  height: 36px;
+  border-radius: var(--r-md);
   display: grid;
   place-items: center;
   flex-shrink: 0;
@@ -664,200 +1015,90 @@ td strong {
 }
 .product-cell small {
   display: block;
-  color: #a0a791;
-  font-size: 9px;
-  margin-top: 4px;
+  margin-top: 2px;
 }
 .unit {
-  color: #9da58f;
-  font-size: 10px;
+  color: var(--ink-muted);
+  font-size: var(--fs-12);
   font-weight: 400;
 }
-.pill {
-  font-size: 9px;
-  padding: 4px 8px;
-  border-radius: 5px;
-  display: inline-flex;
-  align-items: center;
-  white-space: nowrap;
-}
-.pill.neutral {
-  background: #f1f2e9;
-  color: #838e76;
-}
-.bordered {
-  border: 1px solid #e8ebdf;
-  width: 26px;
-  height: 26px;
-}
-.bordered svg {
-  width: 14px;
-  height: 14px;
-}
 .incoming {
   display: block;
-  color: #738967;
-  font-size: 9px;
-  margin-top: 3px;
-}
-.right-stack {
-  display: flex;
-  flex-direction: column;
-  gap: 18px;
-}
-.count-bubble {
-  font-size: 10px;
-  border-radius: 50%;
-  background: #f5e7dc;
-  color: #b0805a;
-  width: 21px;
-  height: 21px;
-  display: grid;
-  place-items: center;
-}
-.message-preview {
-  display: block;
-  text-align: left;
-  border: 0;
-  background: transparent;
-  padding: 0 23px 20px;
-  width: 100%;
+  color: var(--pistacho);
+  font-size: var(--fs-12);
+  margin-top: 2px;
 }
-.message-top {
+.table-footer {
+  padding: var(--s-3) var(--s-5);
+  color: var(--ink-muted);
+  font-size: var(--fs-12);
   display: flex;
-  gap: 10px;
-  align-items: center;
-  margin-bottom: 14px;
-}
-.supplier-avatar {
-  display: inline-flex;
-  width: 35px;
-  height: 35px;
-  align-items: center;
-  justify-content: center;
-  border-radius: 10px;
-  font-size: 11px;
-  font-weight: 600;
-  flex-shrink: 0;
-}
-.message-top strong {
-  font-size: 11px;
-  color: #546149;
+  justify-content: space-between;
+  gap: var(--s-3);
+  flex-wrap: wrap;
+  border-top: 1px solid var(--border-soft);
 }
-.message-top small {
+.movement-reason {
   display: block;
-  font-size: 8px;
-  color: #a0a68f;
+  max-width: 44ch;
+  white-space: normal;
   margin-top: 2px;
 }
-.unread-dot {
-  width: 6px;
-  height: 6px;
-  border-radius: 50%;
-  background: #b99263;
-  flex-shrink: 0;
-  margin-left: auto;
-}
-.message-preview p {
-  font-size: 11px;
-  color: #828b77;
-  margin: 9px 0 13px;
-  line-height: 1.7;
-}
-.text-link {
-  font-size: 10px;
-  color: #638059;
-  display: flex;
-  align-items: center;
-  gap: 6px;
-}
-.text-link svg {
-  width: 12px;
-  height: 12px;
-}
-.full-link {
-  width: 100%;
-  border: 0;
-  border-top: 1px solid #ecefe3;
-  background: #fcfcf6;
-  color: #7c8871;
-  padding: 12px;
-  font-size: 10px;
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  gap: 8px;
-}
-.full-link svg {
-  width: 12px;
-  height: 12px;
-}
-.tip-card {
-  background: #edf0e4;
-  border: 1px solid #e3e8d7;
-  border-radius: 12px;
-  padding: 20px;
-}
-.tip-card h3 {
-  margin: 10px 0 6px;
-  color: #667657;
-  font-size: 13px;
-}
-.tip-card p {
-  font-size: 10px;
-  line-height: 1.7;
-  color: #8b987c;
-  max-width: 270px;
-}
-footer {
-  display: flex;
-  justify-content: space-between;
-  font-size: 9px;
-  color: #a7ad9b;
-  padding: 20px 38px 24px;
-  max-width: 1640px;
-  margin: auto;
-}
+
+/* ---------- Barra de herramientas, pestañas y búsqueda ---------- */
 .toolbar {
-  padding: 18px 20px;
+  padding: var(--s-4) var(--s-5);
   display: flex;
-  gap: 14px;
+  gap: var(--s-3);
   align-items: center;
   justify-content: space-between;
-  border-bottom: 1px solid #e9ece2;
+  flex-wrap: wrap;
+  border-bottom: 1px solid var(--border-soft);
 }
 .tabs {
   display: flex;
-  gap: 3px;
+  gap: 2px;
   flex-wrap: wrap;
 }
 .tab {
   border: 0;
   background: transparent;
-  font-size: 11px;
-  padding: 8px 10px;
-  color: #89937c;
-  border-radius: 7px;
+  font-size: var(--fs-13);
+  font-weight: 600;
+  padding: 0 var(--s-3);
+  min-height: var(--control-sm);
+  color: var(--ink-soft);
+  border-radius: var(--r-sm);
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+}
+.tab:hover {
+  background: var(--crema-hundida);
+  color: var(--ink);
 }
 .tab.selected {
-  background: #eaf0e3;
-  color: #526a48;
+  background: var(--accent-soft);
+  color: var(--accent-text);
 }
 .tab span {
-  font-size: 9px;
-  background: #dce7d5;
-  padding: 1px 4px;
-  border-radius: 4px;
+  font-size: var(--fs-12);
+  background: var(--surface);
+  border: 1px solid var(--border);
+  padding: 0 6px;
+  border-radius: 999px;
+  line-height: 1.5;
 }
 .search {
-  border: 1px solid #e2e7d8;
-  border-radius: 8px;
+  border: 1px solid var(--border);
+  border-radius: var(--r-md);
   display: flex;
   align-items: center;
-  padding: 0 10px;
-  gap: 7px;
-  color: #a0aa91;
-  background: #fff;
+  padding: 0 var(--s-3);
+  gap: var(--s-2);
+  color: var(--ink-muted);
+  background: var(--surface);
+  min-height: var(--control-h);
 }
 .search svg {
   width: 16px;
@@ -865,1244 +1106,1660 @@ footer {
 }
 .search input {
   border: 0;
-  padding: 9px 0;
+  padding: 0;
   outline: none;
-  width: 150px;
-  font-size: 11px;
+  width: 200px;
+  font-size: var(--fs-14);
   background: transparent;
-  color: #5f6d51;
+  color: var(--ink);
 }
-.table-footer {
-  padding: 14px 20px;
-  color: #8d9c93;
-  font-size: 10px;
+.search:focus-within {
+  border-color: var(--pistacho);
+  box-shadow: 0 0 0 3px var(--pistacho-claro);
+}
+.search input:focus-visible {
+  outline: none;
+}
+.tracking-tabs {
   display: flex;
-  justify-content: space-between;
-  border-top: 1px solid #eef0e6;
+  gap: var(--s-2);
+  flex-wrap: wrap;
 }
-.notice {
+
+/* ---------- Campos y formularios ---------- */
+.field {
   display: flex;
-  align-items: center;
-  gap: 13px;
-  background: #f2f0e6;
-  border: 1px solid #e8e4d4;
-  border-radius: 10px;
-  padding: 16px 19px;
-  color: #8a8063;
-  margin-bottom: 22px;
+  flex-direction: column;
+  gap: 6px;
+  margin-bottom: var(--s-4);
+  font-size: var(--fs-13);
+  font-weight: 600;
+  color: var(--ink-soft);
 }
-.notice strong {
-  font-size: 12px;
-  display: block;
+.field input,
+.field select,
+.field textarea,
+.inline-input,
+.quantity {
+  width: 100%;
+  border: 1px solid var(--border);
+  border-radius: var(--r-md);
+  padding: 0 var(--s-3);
+  min-height: var(--control-h);
+  background: #fff;
+  color: var(--ink);
+  font-size: var(--fs-14);
+  font-weight: 400;
 }
-.notice span {
-  font-size: 11px;
-  display: block;
-  margin-top: 3px;
+.field textarea,
+.ai-editor {
+  min-height: 112px;
+  padding: var(--s-3);
+  resize: vertical;
+  line-height: var(--lh-text);
 }
-.notice.subtle {
-  background: #edf1e7;
-  border-color: #e1e8d7;
-  color: #7c8b6d;
+.field input:focus-visible,
+.field select:focus-visible,
+.field textarea:focus-visible,
+.inline-input:focus-visible,
+.quantity:focus-visible {
+  outline: 3px solid var(--pistacho-claro);
+  outline-offset: 0;
+  border-color: var(--pistacho);
 }
-.notice.inline {
-  margin: 15px 0 0;
-  font-size: 11px;
+.field input[readonly],
+.field textarea[readonly] {
+  background: var(--crema-hundida);
 }
-.purchase-grid {
-  display: grid;
-  grid-template-columns: minmax(0, 1fr) 300px;
-  gap: 22px;
-}
-.cart-lines {
-  padding: 0 20px;
-}
-.cart-line {
-  display: flex;
-  align-items: center;
-  gap: 12px;
-  padding: 18px 0;
-  border-bottom: 1px solid #eff0e6;
+.field input[type="file"] {
+  padding: 8px var(--s-3);
 }
-.cart-desc {
-  flex: 1;
-  min-width: 0;
+.field.short {
+  max-width: 240px;
 }
-.cart-desc strong {
-  font-size: 12px;
+.field > span {
+  font-weight: 600;
 }
-.cart-desc small {
-  display: block;
-  color: #97a087;
-  font-size: 9px;
-  margin-top: 5px;
+.inline-input {
+  width: 120px;
+  min-height: 36px;
+  padding: 0 var(--s-2);
 }
-.cart-line > strong {
-  font-size: 12px;
-  white-space: nowrap;
-  min-width: 55px;
-  text-align: right;
+.inline-input.wide {
+  width: 100%;
 }
 .quantity {
-  width: 54px;
-  padding: 7px;
-  border: 1px solid #dfe4d5;
-  border-radius: 6px;
-  background: #fff;
-  color: #5b6b4d;
-  font-size: 12px;
-}
-.panel-bottom {
-  padding: 18px 20px;
+  width: 72px;
+  min-height: 36px;
+  padding: 0 var(--s-2);
+  text-align: center;
 }
-.order-summary {
-  padding: 24px;
-  align-self: start;
+.form-grid {
+  display: grid;
+  grid-template-columns: 1fr 1fr;
+  gap: 0 var(--s-4);
 }
-.summary-row {
+.check,
+.check-label {
   display: flex;
-  justify-content: space-between;
-  font-size: 11px;
-  color: #8a947d;
-  margin-top: 20px;
+  gap: var(--s-2);
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
+  align-items: flex-start;
+  margin-bottom: var(--s-4);
+  line-height: 1.5;
 }
-.summary-row strong {
-  color: #59684e;
+.check input,
+.check-label input {
+  width: 16px;
+  height: 16px;
+  margin: 2px 0 0;
+  accent-color: var(--pistacho);
+  flex: none;
 }
-.summary-total {
-  border-top: 1px solid #e7eadd;
-  margin-top: 20px;
-  padding-top: 20px;
-  display: flex;
-  justify-content: space-between;
+.ingredient-row {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) 120px auto;
+  gap: var(--s-2);
   align-items: center;
-  font-size: 12px;
-}
-.summary-total strong {
-  font-size: 22px;
-  color: #48633f;
+  margin-bottom: var(--s-2);
 }
-.order-summary p {
-  font-size: 10px;
-  line-height: 1.7;
-  color: #98a18a;
-  margin: 16px 0;
+.ingredient-row select,
+.ingredient-row input {
+  width: 100%;
+  border: 1px solid var(--border);
+  border-radius: var(--r-md);
+  padding: 0 var(--s-3);
+  min-height: var(--control-h);
+  background: #fff;
+  font-weight: 400;
 }
-.order-summary > small {
-  display: block;
-  text-align: center;
-  color: #8d9c93;
-  font-size: 9px;
-  margin-top: 12px;
+.review-total {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--s-3);
+  background: var(--pistacho-suave);
+  padding: var(--s-4) var(--s-5);
+  margin-top: var(--s-4);
+  border-radius: var(--r-md);
+  font-size: var(--fs-14);
 }
-.orders-panel {
-  margin-top: 24px;
+.review-total strong {
+  font-size: var(--fs-22);
+  color: var(--pistacho);
 }
-.order-row {
+.upload-zone {
   display: flex;
   align-items: center;
-  gap: 24px;
-  padding: 20px 24px;
-  border-top: 1px solid #eff0e7;
-}
-.order-row > div:first-child {
-  flex: 1;
+  flex-direction: column;
+  border: 1px dashed #b7c6a6;
+  background: var(--pistacho-suave);
+  border-radius: var(--r-md);
+  padding: var(--s-5);
+  color: var(--ink-soft);
+  gap: var(--s-2);
+  margin-bottom: var(--s-4);
+  text-align: center;
 }
-.order-number {
-  font-size: 9px;
-  color: #98a38a;
-  letter-spacing: 1px;
+.upload-zone > svg {
+  color: var(--pistacho);
 }
-.order-row h3 {
-  margin: 3px 0;
+.upload-zone strong {
+  font-size: var(--fs-14);
+  color: var(--ink);
 }
-.order-row small {
-  font-size: 10px;
-  color: #97a18a;
+.upload-zone span {
+  font-size: var(--fs-12);
+  color: var(--ink-muted);
 }
-.order-status {
-  min-width: 145px;
+.upload-zone input {
+  max-width: 100%;
+  font-size: var(--fs-13);
+  margin-top: var(--s-1);
 }
-.order-status small {
+.photo-preview {
+  max-width: 100%;
+  max-height: 240px;
   display: block;
-  margin-top: 5px;
+  margin: 0 auto var(--s-4);
+  border-radius: var(--r-md);
+  border: 1px solid var(--border);
+}
+.detection-status {
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
+  background: var(--pistacho-suave);
+  border-radius: var(--r-md);
+  padding: var(--s-2) var(--s-3);
+  margin-bottom: var(--s-4);
+}
+details {
+  margin-bottom: var(--s-4);
+}
+summary {
+  cursor: pointer;
+  font-size: var(--fs-13);
+  font-weight: 600;
+  color: var(--ink-soft);
+  padding: var(--s-1) 0;
 }
-.received-check {
-  font-size: 11px;
-  color: #758d64;
-  display: flex;
-  gap: 6px;
+details[open] summary {
+  margin-bottom: var(--s-2);
 }
-.inbox-layout {
-  display: grid;
-  grid-template-columns: 330px minmax(0, 1fr);
-  border: 1px solid #e6dfd2;
-  border-radius: 12px;
-  background: #fffdf9;
-  min-height: 560px;
-  overflow: hidden;
+#form-error:empty {
+  display: none;
 }
-.conversation-list {
-  border-right: 1px solid #e6dfd2;
-  background: #fbfcf6;
+#form-error:not(:empty) {
+  padding: var(--s-3);
+  background: var(--rosa-claro);
+  border-radius: var(--r-md);
+  color: var(--rosa-texto);
+  font-size: var(--fs-13);
+  margin-top: var(--s-2);
 }
-.conversation-heading {
-  display: flex;
-  justify-content: space-between;
-  padding: 21px;
-  font-size: 11px;
-  color: #7e8d6c;
-  border-bottom: 1px solid #ebe4d8;
+
+/* ---------- Diálogos y aviso flotante ---------- */
+dialog {
+  padding: 0;
+  border: 1px solid var(--border);
+  border-radius: var(--r-xl);
+  width: 580px;
+  max-width: calc(100vw - 48px);
+  max-height: calc(100vh - 48px);
+  background: var(--surface);
+  color: var(--ink);
+  box-shadow: var(--shadow-lg);
 }
-.conversation-heading span {
-  color: #a7b198;
+dialog::backdrop {
+  background: rgba(30, 42, 47, 0.38);
+  backdrop-filter: blur(3px);
 }
-.conversation {
+#modal-form {
   display: flex;
-  align-items: flex-start;
-  text-align: left;
-  gap: 11px;
-  width: 100%;
-  padding: 22px 18px;
-  border: 0;
-  border-bottom: 1px solid #ebe4d8;
-  background: transparent;
-}
-.conversation.selected {
-  background: #ecf1e5;
-  border-left: 3px solid #82966e;
-  padding-left: 15px;
-}
-.conversation > div {
-  min-width: 0;
-  flex: 1;
+  flex-direction: column;
+  max-height: calc(100vh - 48px);
 }
-.conversation-title {
+.modal-heading {
+  padding: var(--s-5) var(--s-5) var(--s-4);
   display: flex;
-  justify-content: space-between;
-  gap: 8px;
-}
-.conversation-title strong {
-  font-size: 11px;
-  color: #60734e;
+  gap: var(--s-4);
+  align-items: flex-start;
+  border-bottom: 1px solid var(--border-soft);
+  flex: none;
 }
-.conversation-title small {
-  font-size: 9px;
-  color: #8d9c93;
+.modal-heading > div {
+  flex: 1;
+  min-width: 0;
 }
-.conversation p {
-  font-size: 11px;
-  color: #919d83;
-  white-space: nowrap;
-  overflow: hidden;
-  text-overflow: ellipsis;
-  margin: 5px 0 8px;
+.modal-heading h2 {
+  font-size: var(--fs-22);
+  font-family: var(--font-serif);
+  font-weight: 500;
+  color: var(--ink-title);
 }
-.mini-status {
-  font-size: 9px;
-  color: #af8662;
+.modal-heading p {
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
+  margin-top: var(--s-2);
 }
-.detail-heading {
-  padding: 25px;
-  display: flex;
-  gap: 12px;
-  align-items: center;
-  border-bottom: 1px solid #edf0e5;
+.modal-body {
+  padding: var(--s-5);
+  overflow: auto;
+  flex: 1 1 auto;
 }
-.detail-heading > div {
-  flex: 1;
+.modal-body > p {
+  font-size: var(--fs-14);
+  color: var(--ink-soft);
+  margin-bottom: var(--s-3);
 }
-.detail-heading h2 {
-  font-size: 15px;
+.modal-body > p:last-child {
+  margin-bottom: 0;
 }
-.detail-heading p {
-  font-size: 9px;
-  color: #9aa68b;
-  margin-top: 3px;
+.modal-body .field:last-child {
+  margin-bottom: 0;
 }
-.message-body {
-  padding: 28px;
+.modal-body .path + .path {
+  margin-top: var(--s-2);
 }
-.message-label {
-  font-size: 8px;
-  letter-spacing: 1.5px;
-  color: #9ba68e;
+.modal-footer {
+  padding: var(--s-4) var(--s-5);
+  border-top: 1px solid var(--border-soft);
   display: flex;
-  align-items: center;
-  gap: 6px;
-  margin-bottom: 12px;
-}
-.message-label svg {
-  width: 14px;
-  height: 14px;
-}
-.message-bubble {
-  background: #f2f4eb;
-  border: 1px solid #e8ecdf;
-  padding: 20px;
-  border-radius: 0 12px 12px 12px;
-  font-size: 13px;
-  line-height: 1.8;
-  color: #647456;
-  white-space: pre-wrap;
+  gap: var(--s-2);
+  justify-content: flex-end;
+  background: var(--crema);
+  flex: none;
 }
-.interpretation {
-  border: 1px solid #e4e8dc;
-  border-radius: 10px;
-  padding: 20px;
-  margin: 26px 0;
+#toast {
+  position: fixed;
+  bottom: var(--s-5);
+  left: calc(50% + var(--sidebar) / 2);
+  transform: translate(-50%, 16px);
+  opacity: 0;
+  pointer-events: none;
+  background: var(--tinta);
+  color: var(--crema-panel);
+  padding: var(--s-3) var(--s-5);
+  border-radius: var(--r-md);
+  box-shadow: var(--shadow-md);
+  font-size: var(--fs-13);
+  z-index: 100;
+  transition: 0.2s;
+  max-width: min(640px, calc(100vw - var(--sidebar) - 48px));
 }
-.interpretation p {
-  font-size: 12px;
-  line-height: 1.7;
-  color: #849575;
-  margin: 8px 0 13px;
+#toast.show {
+  opacity: 1;
+  transform: translate(-50%, 0);
 }
-.muted {
-  font-size: 11px;
-  color: #99a68d;
+
+/* ---------- Estados vacíos ---------- */
+.empty {
+  text-align: center;
+  padding: var(--s-7) var(--s-5);
+  color: var(--ink-soft);
+  font-size: var(--fs-14);
+  max-width: none;
 }
-.message-actions {
-  display: flex;
-  gap: 8px;
-  flex-wrap: wrap;
+.empty > svg {
+  width: 32px;
+  height: 32px;
+  color: var(--pistacho);
+  margin-bottom: var(--s-3);
 }
-.fineprint {
-  font-size: 10px !important;
-  color: #98a48a !important;
-  line-height: 1.7;
-  margin-top: 17px !important;
+.empty h3 {
+  color: var(--ink);
+  margin-bottom: var(--s-1);
 }
-.supplier-grid {
-  display: grid;
-  grid-template-columns: repeat(2, minmax(0, 1fr));
-  gap: 20px;
-  margin-bottom: 24px;
+.empty p {
+  margin: 0 auto;
+  color: var(--ink-muted);
 }
-.supplier-card {
-  padding: 26px;
+.empty.compact {
+  padding: var(--s-5);
 }
-.supplier-avatar.large {
-  width: 48px;
-  height: 48px;
-  font-size: 16px;
-  border-radius: 14px;
-  margin-bottom: 17px;
+.panel.empty {
+  display: block;
 }
-.supplier-card h2 {
-  font-size: 19px;
+
+/* ---------- Resumen: hero, cifras, tablero ---------- */
+.hero {
+  background: linear-gradient(
+    120deg,
+    var(--pistacho-oscuro) 0%,
+    var(--pistacho) 48%,
+    #4f7d5f 100%
+  );
+  border-radius: var(--r-xl);
+  min-height: 256px;
+  display: flex;
+  position: relative;
+  overflow: hidden;
+  color: var(--crema-panel);
 }
-.supplier-card > p {
-  font-size: 12px;
-  color: #8e9c7f;
-  margin: 5px 0 22px;
+.hero-copy {
+  padding: var(--s-6) var(--s-6) var(--s-6);
+  z-index: 1;
+  position: relative;
+  max-width: 60%;
 }
-.supplier-info {
+.hero-label {
+  font-size: var(--fs-12);
+  letter-spacing: 1.8px;
+  font-weight: 650;
+  color: #d8e6c8;
   display: flex;
-  gap: 9px;
+  gap: var(--s-2);
   align-items: center;
-  color: #8d9a7d;
-  font-size: 11px;
-  margin: 12px 0;
 }
-.supplier-info svg {
-  width: 15px;
-  height: 15px;
+.hero-label .dot {
+  background: var(--azafran);
 }
-.supplier-card-footer {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  margin-top: 24px;
-  border-top: 1px solid #ebe4d8;
-  padding-top: 20px;
+.hero h2 {
+  font-family: var(--font-serif);
+  font-weight: 400;
+  font-size: 34px;
+  line-height: 1.15;
+  letter-spacing: -0.6px;
+  margin-top: var(--s-3);
+  color: var(--crema-panel);
 }
-.activity-panel {
-  padding: 15px 28px;
+.hero p {
+  font-size: var(--fs-14);
+  line-height: 1.6;
+  color: #eef4e6;
+  margin-top: var(--s-3);
 }
-.activity-row {
-  display: flex;
-  gap: 18px;
-  padding: 18px 0;
-  border-bottom: 1px solid #edf0e6;
+.hero .btn.cream {
+  margin-top: var(--s-4);
 }
-.activity-row:last-child {
-  border: 0;
+.hero-art {
+  position: absolute;
+  right: 32px;
+  top: 0;
+  width: 380px;
+  height: 100%;
+  pointer-events: none;
 }
-.activity-dot {
-  width: 8px;
-  height: 8px;
-  flex: none;
-  border: 2px solid #a4b491;
+.art-orbit {
+  position: absolute;
+  width: 300px;
+  height: 300px;
   border-radius: 50%;
-  margin-top: 6px;
-}
-.activity-row p {
-  font-size: 12px;
-  color: #768967;
+  border: 1px solid rgba(255, 253, 249, 0.22);
+  right: 4px;
+  top: 18px;
 }
-.activity-row small {
-  font-size: 10px;
-  color: #a2ae95;
-  display: block;
-  margin-top: 5px;
+.art-orbit:after {
+  content: "";
+  position: absolute;
+  inset: 17px;
+  border-radius: 50%;
+  border: 1px solid rgba(255, 253, 249, 0.14);
 }
-.settings-grid {
-  display: grid;
-  grid-template-columns: repeat(2, minmax(0, 1fr));
-  gap: 20px;
+.scoop {
+  position: absolute;
+  width: 107px;
+  height: 101px;
+  border-radius: 49% 51% 42% 44%;
+  box-shadow:
+    inset -10px -10px 22px rgba(0, 0, 0, 0.07),
+    inset 6px 5px 15px rgba(255, 255, 255, 0.13);
 }
-.settings-card {
-  padding: 26px;
+.scoop-one {
+  background: #b9cf98;
+  left: 95px;
+  top: 52px;
+  transform: rotate(-14deg);
 }
-.settings-card h2 {
-  margin: 15px 0 10px;
+.scoop-two {
+  background: #efb7c3;
+  left: 175px;
+  top: 54px;
+  transform: rotate(14deg);
 }
-.settings-card > p {
-  font-size: 12px;
-  line-height: 1.8;
-  color: #8a9a7b;
-  margin: 12px 0;
+.scoop-three {
+  background: #f1cf7e;
+  left: 132px;
+  top: 105px;
+  z-index: 1;
 }
-.path-label {
-  font-size: 8px;
-  letter-spacing: 1.5px;
-  color: #a0ad91;
-  display: block;
-  margin: 20px 0 8px;
+.gelato-cup {
+  position: absolute;
+  top: 155px;
+  left: 91px;
+  width: 190px;
+  height: 100px;
+  background: linear-gradient(100deg, #eceadb, #fffdf9 45%, #e6e4d6);
+  clip-path: polygon(0 0, 100% 0, 84% 100%, 17% 100%);
+  border-radius: 7px 7px 35px 35px;
+  z-index: 2;
+  display: flex;
+  justify-content: center;
+  color: #5a6a4e;
+  font-family: var(--font-serif);
+  font-style: italic;
+  font-size: 62px;
+  box-shadow: inset 0 6px 0 #e3e2d3;
 }
-.path {
-  display: block;
-  word-break: break-all;
-  background: #f2efe6;
-  padding: 12px;
-  border-radius: 7px;
-  color: #6e825d;
-  font-size: 11px;
-  white-space: pre-wrap;
+.art-caption {
+  position: absolute;
+  right: 0;
+  top: 44px;
+  font-family: var(--font-serif);
+  font-style: italic;
+  font-size: var(--fs-14);
+  color: #d8e6c8;
+  transform: rotate(6deg);
+  text-align: right;
 }
-.setting-actions {
-  display: flex;
-  gap: 8px;
-  margin-top: 20px;
-  flex-wrap: wrap;
+.art-dot {
+  position: absolute;
+  width: 10px;
+  height: 10px;
+  background: var(--azafran);
+  border-radius: 50%;
+  left: 50px;
+  top: 99px;
+  opacity: 0.8;
 }
-.feature-list {
-  padding: 0;
-  list-style: none;
-  color: #8a9d79;
-  font-size: 11px;
+.stats {
+  display: grid;
+  grid-template-columns: repeat(4, minmax(0, 1fr));
+  gap: var(--s-4);
 }
-.feature-list li {
+.stat {
+  background: var(--surface);
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
+  padding: var(--s-4) var(--s-4);
   display: flex;
-  gap: 9px;
-  margin: 12px 0;
+  gap: var(--s-3);
+  min-width: 0;
+  box-shadow: var(--shadow-sm);
 }
-.feature-list svg {
-  width: 15px;
-  height: 15px;
+.stat > div {
+  min-width: 0;
 }
-.photo-grid {
+.stat p {
+  color: var(--ink-muted);
+  font-size: var(--fs-13);
+  margin-bottom: var(--s-1);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+.stat strong {
+  display: block;
+  font-family: var(--font-serif);
+  font-size: var(--fs-32);
+  font-weight: 500;
+  line-height: 1.1;
+  letter-spacing: -0.5px;
+  color: var(--ink);
+}
+.stat .money-value {
+  font-size: 26px;
+  line-height: 1.35;
+  white-space: nowrap;
+}
+.stat small {
+  display: block;
+  margin-top: var(--s-1);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+.dashboard-grid {
   display: grid;
-  grid-template-columns: repeat(2, 1fr);
-  gap: 10px;
-  margin-top: 20px;
+  grid-template-columns: minmax(0, 1.8fr) minmax(300px, 1fr);
+  gap: var(--s-5);
+  align-items: start;
 }
-.photo-grid figure {
-  margin: 0;
-  min-width: 0;
+.right-stack {
+  display: flex;
+  flex-direction: column;
+  gap: var(--s-4);
 }
-.photo-grid img {
+.message-preview {
+  display: block;
+  text-align: left;
+  border: 0;
+  background: transparent;
+  padding: var(--s-2) var(--s-5) var(--s-4);
   width: 100%;
-  height: 120px;
-  object-fit: cover;
-  border: 1px solid #e3e9d9;
-  border-radius: 7px;
+  border-radius: 0;
 }
-.photo-grid figcaption {
-  font-size: 10px;
-  color: #839771;
-  word-break: break-word;
+.message-preview:hover {
+  background: var(--crema);
 }
-.photo-grid small {
-  display: block;
-  font-size: 9px;
+.message-top {
+  display: flex;
+  gap: var(--s-3);
+  align-items: center;
+  margin-bottom: var(--s-3);
 }
-.empty {
-  text-align: center;
-  padding: 45px 20px;
-  color: #8d9c93;
-  font-size: 12px;
+.message-top > div {
+  min-width: 0;
 }
-.empty > svg {
-  width: 30px;
-  height: 30px;
-  color: #9eb58e;
-  margin-bottom: 10px;
+.message-top strong {
+  font-size: var(--fs-14);
+  color: var(--ink);
+  display: block;
 }
-.empty h3 {
-  color: #758b63;
-  margin-bottom: 8px;
+.message-top small {
+  display: block;
+  margin-top: 1px;
 }
-.empty p {
-  margin-bottom: 18px;
+.message-preview p {
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
+  margin: var(--s-2) 0 var(--s-3);
+  line-height: 1.5;
+  display: -webkit-box;
+  -webkit-line-clamp: 3;
+  -webkit-box-orient: vertical;
+  overflow: hidden;
 }
-.empty.compact {
-  padding: 25px;
+.text-link {
+  font-size: var(--fs-13);
+  font-weight: 600;
+  color: var(--pistacho);
+  display: flex;
+  align-items: center;
+  gap: 6px;
 }
-.boot {
-  margin: auto;
+.text-link svg {
+  width: 14px;
+  height: 14px;
 }
-.sr-only {
-  position: absolute;
-  width: 1px;
-  height: 1px;
-  padding: 0;
-  overflow: hidden;
-  clip: rect(0, 0, 0, 0);
-  white-space: nowrap;
+.full-link {
+  width: 100%;
   border: 0;
+  border-top: 1px solid var(--border-soft);
+  background: var(--crema);
+  color: var(--pistacho);
+  padding: var(--s-3);
+  min-height: 44px;
+  font-size: var(--fs-13);
+  font-weight: 600;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  gap: var(--s-2);
 }
-dialog {
-  padding: 0;
-  border: 1px solid #e4e9da;
-  border-radius: 15px;
-  width: 560px;
-  max-width: 90vw;
-  max-height: 90vh;
-  background: #fffdf9;
-  color: #43543a;
-  box-shadow: 0 30px 100px #29352225;
+.full-link:hover {
+  background: var(--pistacho-suave);
 }
-dialog::backdrop {
-  background: #26332355;
-  backdrop-filter: blur(3px);
+.full-link svg {
+  width: 14px;
+  height: 14px;
 }
-.modal-heading {
-  padding: 24px;
+.inbox-preview .panel-heading {
+  align-items: center;
+}
+.tip-card {
+  background: var(--pistacho-suave);
+  border: 1px solid var(--pistacho-claro);
+  border-radius: var(--r-lg);
+  padding: var(--s-5);
+}
+.tip-card h3 {
+  margin: var(--s-3) 0 var(--s-1);
+  color: var(--pistacho);
+}
+.tip-card p {
+  font-size: var(--fs-13);
+  line-height: 1.55;
+  color: var(--ink-soft);
+}
+
+/* ---------- Compras: carrito y resumen ---------- */
+.purchase-grid {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) 320px;
+  gap: var(--s-5);
+  align-items: start;
+}
+.cart-lines {
+  padding: 0 var(--s-5);
+}
+.cart-line {
   display: flex;
-  gap: 15px;
-  align-items: flex-start;
-  border-bottom: 1px solid #ebe4d8;
+  align-items: center;
+  gap: var(--s-3);
+  padding: var(--s-4) 0;
+  border-bottom: 1px solid var(--border-soft);
 }
-.modal-heading > div {
+.cart-desc {
   flex: 1;
+  min-width: 0;
 }
-.modal-heading h2 {
-  font-size: 21px;
+.cart-desc strong {
+  font-size: var(--fs-14);
+  display: block;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
-.modal-heading p {
-  font-size: 11px;
-  color: #96a488;
-  margin-top: 7px;
-  line-height: 1.7;
+.cart-desc small {
+  display: block;
+  margin-top: 2px;
 }
-.modal-body {
-  padding: 24px;
+.cart-line > strong {
+  font-size: var(--fs-14);
+  white-space: nowrap;
+  min-width: 72px;
+  text-align: right;
 }
-.modal-body > p {
-  font-size: 12px;
-  color: #819571;
+.panel-bottom {
+  padding: var(--s-4) var(--s-5);
+  border-top: 1px solid var(--border-soft);
 }
-.modal-footer {
-  padding: 18px 24px;
-  border-top: 1px solid #ebe4d8;
+.order-summary {
+  padding: var(--s-5);
+}
+.summary-row {
   display: flex;
-  gap: 9px;
-  justify-content: flex-end;
-  background: #fdfaf4;
+  justify-content: space-between;
+  gap: var(--s-3);
+  font-size: var(--fs-13);
+  color: var(--ink-muted);
+  margin-top: var(--s-3);
 }
-.field {
+.summary-row strong {
+  color: var(--ink);
+}
+.summary-total {
+  border-top: 1px solid var(--border-soft);
+  margin-top: var(--s-4);
+  padding-top: var(--s-4);
   display: flex;
-  flex-direction: column;
-  gap: 7px;
-  margin-bottom: 16px;
-  font-size: 11px;
-  color: #819271;
+  justify-content: space-between;
+  align-items: baseline;
+  gap: var(--s-3);
+  font-size: var(--fs-14);
 }
-.field input,
-.field select,
-.field textarea {
-  width: 100%;
-  border: 1px solid #dfe6d4;
-  border-radius: 7px;
-  padding: 10px 11px;
-  background: #fff;
-  color: #556b46;
-  font-size: 12px;
+.summary-total strong {
+  font-family: var(--font-serif);
+  font-size: var(--fs-22);
+  font-weight: 500;
+  color: var(--pistacho);
 }
-.field textarea {
-  min-height: 115px;
-  resize: vertical;
-  line-height: 1.7;
+.order-summary p {
+  font-size: var(--fs-13);
+  line-height: 1.55;
+  color: var(--ink-muted);
+  margin: var(--s-4) 0;
 }
-.form-grid {
+.order-summary > small {
+  display: block;
+  text-align: center;
+  margin-top: var(--s-2);
+}
+
+/* ---------- Control de entregas ---------- */
+.orders-panel {
+  min-width: 0;
+}
+.tracking-intro {
   display: grid;
-  grid-template-columns: 1fr 1fr;
-  gap: 0 15px;
+  grid-template-columns: minmax(0, 1.5fr) minmax(280px, 1fr);
+  gap: var(--s-5);
+  align-items: start;
+  margin-bottom: var(--s-4);
 }
-.review-total {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  background: #f0f4e9;
-  padding: 20px;
-  margin-top: 16px;
-  border-radius: 8px;
-  font-size: 12px;
+.tracking-intro h2 {
+  font-family: var(--font-serif);
+  font-weight: 500;
+  font-size: 26px;
+  color: var(--ink-title);
+  margin: var(--s-1) 0 var(--s-2);
 }
-.review-total strong {
-  font-size: 24px;
+.tracking-intro p {
+  color: var(--ink-soft);
 }
-.check-label {
+.tracking-example {
+  background: var(--accent-soft);
+  border-radius: var(--r-lg);
+  padding: var(--s-4) var(--s-5);
+  color: var(--ink-soft);
+}
+.tracking-example strong {
+  color: var(--ink);
+}
+.tracking-example p {
+  margin: var(--s-1) 0 var(--s-2);
+}
+.delivery-list {
+  display: grid;
+  gap: var(--s-4);
+  margin-top: var(--s-4);
+}
+.delivery-card {
+  background: var(--surface);
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
+  overflow: hidden;
+  box-shadow: var(--shadow-sm);
+}
+.delivery-card > header {
   display: flex;
-  gap: 10px;
-  font-size: 11px;
-  color: #8b9e7c;
+  justify-content: space-between;
   align-items: flex-start;
+  gap: var(--s-3);
+  padding: var(--s-4) var(--s-5) var(--s-2);
+  flex-wrap: wrap;
 }
-.upload-zone {
+.delivery-card > header > div {
+  min-width: 0;
+}
+.order-number {
+  font-size: var(--fs-12);
+  font-weight: 650;
+  color: var(--ink-muted);
+  letter-spacing: 0.6px;
+  text-transform: uppercase;
+}
+.delivery-card h3 {
+  font-family: var(--font-serif);
+  font-weight: 500;
+  font-size: var(--fs-22);
+  margin: var(--s-1) 0 0;
+  color: var(--ink-title);
+}
+.delivery-progress {
   display: flex;
-  align-items: center;
-  flex-direction: column;
-  border: 1px dashed #bcccaa;
-  border-radius: 10px;
-  padding: 24px;
-  color: #8fa07e;
-  gap: 10px;
-  margin-bottom: 20px;
+  justify-content: space-between;
+  gap: var(--s-3);
+  flex-wrap: wrap;
+  padding: var(--s-2) var(--s-5) var(--s-4);
+  color: var(--ink-soft);
+  font-size: var(--fs-13);
 }
-.upload-zone strong {
-  font-size: 12px;
+.delivery-progress strong {
+  color: var(--ink);
 }
-.upload-zone span {
-  font-size: 10px;
+.delivery-table {
+  width: 100%;
+  border-collapse: collapse;
 }
-.upload-zone input {
-  max-width: 100%;
-  font-size: 11px;
+.delivery-table th {
+  padding: var(--s-3) var(--s-5);
 }
-.photo-preview {
-  max-width: 100%;
-  max-height: 230px;
+.delivery-table td {
+  padding: var(--s-3) var(--s-5);
+}
+.delivery-table small,
+.delivery-next small {
   display: block;
-  margin: 0 auto 20px;
-  border-radius: 8px;
+  margin-top: 2px;
 }
-#form-error {
-  color: #a25d45;
-  font-size: 11px;
+.order-replies {
+  padding: var(--s-3) var(--s-5) 0;
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
 }
-#toast {
-  position: fixed;
-  bottom: 26px;
-  left: calc(50% + 90px);
-  transform: translate(-50%, 20px);
-  opacity: 0;
-  pointer-events: none;
-  background: #3f5737;
-  color: #fffdf9;
-  padding: 14px 22px;
-  border-radius: 10px;
-  box-shadow: 0 10px 35px #273b3225;
-  font-size: 12px;
-  z-index: 100;
-  transition: 0.2s;
-  max-width: 620px;
+.order-replies strong {
+  color: var(--ink);
 }
-#toast.show {
-  opacity: 1;
-  transform: translate(-50%, 0);
+.order-replies p {
+  margin: var(--s-2) 0;
+  display: flex;
+  gap: var(--s-2);
+  align-items: baseline;
+  flex-wrap: wrap;
 }
-@media (min-width: 1550px) {
-  .hero {
-    min-height: 280px;
-  }
-  .hero-art {
-    right: 10%;
-    transform: scale(1.08);
-  }
-  .hero-copy {
-    padding: 34px 38px;
-  }
-  .hero h2 {
-    font-size: 39px;
-  }
+.delivery-next {
+  display: flex;
+  justify-content: space-between;
+  gap: var(--s-3) var(--s-4);
+  align-items: center;
+  flex-wrap: wrap;
+  padding: var(--s-4) var(--s-5);
+  background: var(--crema);
+  border-top: 1px solid var(--border-soft);
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
+  margin-top: var(--s-3);
+}
+.delivery-next > div:first-child {
+  min-width: 0;
+  flex: 1 1 320px;
 }
-@media (max-width: 1220px) {
-  .sidebar {
-    width: 210px;
-    padding-left: 13px;
-    padding-right: 13px;
-  }
-  main {
-    margin-left: 210px;
-  }
-  .content {
-    padding: 26px;
-  }
-  .topbar {
-    padding: 0 26px;
-  }
-  .heading-actions {
-    gap: 6px;
-  }
-  .page-heading h1 {
-    font-size: 25px;
-  }
-  .hero-art {
-    right: 0;
-    width: 320px;
-    opacity: 0.85;
-  }
-  .stat {
-    padding: 15px 12px;
-    gap: 9px;
-  }
-  .stat-icon {
-    width: 30px;
-    height: 30px;
-    border-radius: 9px;
-  }
-  .stat p {
-    font-size: 9px;
-  }
-  .stat strong {
-    font-size: 24px;
-  }
-  .stat .money-value {
-    font-size: 18px;
-  }
-  .dashboard-grid {
-    grid-template-columns: minmax(0, 1.6fr) minmax(250px, 1fr);
-  }
-  th,
-  td {
-    padding-left: 13px;
-    padding-right: 13px;
-  }
-  .product-cell {
-    gap: 8px;
-  }
-  .panel-heading {
-    padding: 19px 17px;
-  }
-  .panel-heading h2 {
-    font-size: 15px;
-  }
-  .purchase-grid {
-    grid-template-columns: minmax(0, 1fr) 270px;
-  }
-  .toolbar {
-    flex-wrap: wrap;
-  }
-  .inbox-layout {
-    grid-template-columns: 290px minmax(0, 1fr);
-  }
-  .hero-copy {
-    max-width: 65%;
-  }
+.delivery-next strong {
+  color: var(--ink);
+  font-weight: 600;
 }
-@media (max-width: 1000px) {
-  .sidebar {
-    width: 180px;
-  }
-  .brand {
-    font-size: 19px;
-    gap: 7px;
-  }
-  .brand small {
-    font-size: 7px;
-    letter-spacing: 1px;
-  }
-  .workspace {
-    padding-left: 3px;
-    padding-right: 3px;
-  }
-  .workspace small {
-    font-size: 9px;
-  }
-  main {
-    margin-left: 180px;
-  }
-  .content {
-    padding: 22px;
-  }
-  .page-heading {
-    align-items: flex-start;
-    flex-direction: column;
-  }
-  .dashboard-grid {
-    grid-template-columns: 1fr;
-  }
-  .right-stack {
-    display: grid;
-    grid-template-columns: 1fr 1fr;
-  }
-  .stats {
-    grid-template-columns: 1fr 1fr;
-  }
-  .purchase-grid {
-    grid-template-columns: 1fr;
-  }
-  .order-summary {
-    max-width: none;
-  }
-  .inbox-layout {
-    grid-template-columns: 250px minmax(0, 1fr);
-  }
-  .settings-grid {
-    grid-template-columns: 1fr;
-  }
-  .hero-art {
-    opacity: 0.65;
-  }
-  .cart-desc small {
-    white-space: normal;
-  }
-  footer {
-    padding: 20px 22px;
-  }
-  .supplier-card-footer {
-    gap: 10px;
-    flex-wrap: wrap;
-  }
+.delivery-next .row-actions {
+  flex: none;
+  justify-content: flex-end;
+}
+
+/* ---------- Mensajes ---------- */
+.message-filters {
+  display: grid;
+  grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr) minmax(0, 1fr);
+  gap: var(--s-4);
+}
+.message-filters .field {
+  margin: 0;
+}
+.inbox-layout {
+  display: grid;
+  grid-template-columns: 340px minmax(0, 1fr);
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
+  background: var(--surface);
+  min-height: 560px;
+  overflow: hidden;
+  box-shadow: var(--shadow-sm);
+}
+.conversation-list {
+  border-right: 1px solid var(--border);
+  background: var(--crema);
+  min-width: 0;
+}
+.conversation-heading {
+  display: flex;
+  justify-content: space-between;
+  gap: var(--s-2);
+  padding: var(--s-4) var(--s-4);
+  font-size: var(--fs-13);
+  font-weight: 650;
+  color: var(--ink);
+  border-bottom: 1px solid var(--border-soft);
+}
+.conversation-heading span {
+  color: var(--ink-muted);
+  font-weight: 500;
+}
+.conversation {
+  display: flex;
+  align-items: flex-start;
+  text-align: left;
+  gap: var(--s-3);
+  width: 100%;
+  padding: var(--s-4);
+  border: 0;
+  border-bottom: 1px solid var(--border-soft);
+  background: transparent;
+  box-shadow: inset 3px 0 0 transparent;
 }
-@media (prefers-reduced-motion: reduce) {
-  * {
-    transition: none !important;
-  }
+.conversation:hover {
+  background: var(--surface);
 }
-
-/* Ajustes tras revisión visual: legibilidad y ventanas bajas. */
-.sidebar {
-  overflow-y: auto;
+.conversation.selected {
+  background: var(--accent-soft);
+  box-shadow: inset 3px 0 0 var(--accent);
 }
-.page-heading p,
-.product-cell small,
-.stat p,
-.stat small,
-.table-footer,
-.message-top small,
-.conversation p,
-.fineprint,
-.message-label,
-.settings-card > p,
-.muted {
-  color: #5f6f74 !important;
+.conversation > div {
+  min-width: 0;
+  flex: 1;
 }
-.product-cell small {
-  font-size: 10px;
+.conversation-title {
+  display: flex;
+  justify-content: space-between;
+  gap: var(--s-2);
+  align-items: baseline;
 }
-.message-bubble {
-  font-size: 14px;
+.conversation-title strong {
+  font-size: var(--fs-14);
+  color: var(--ink);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
-.interpretation p {
-  font-size: 13px;
-  color: #697a5c;
+.conversation-title small {
+  flex: none;
 }
 .conversation p {
-  font-size: 12px;
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  margin: var(--s-1) 0 var(--s-1);
+  max-width: none;
 }
-.pill {
-  font-size: 10px;
+.mini-status {
+  font-size: var(--fs-12);
+  color: var(--accent-text);
+  display: block;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
-.message-preview p {
-  font-size: 12px;
-  color: #737f69;
+.message-detail {
+  min-width: 0;
 }
-.stat p {
-  font-size: 11px;
+.detail-heading {
+  padding: var(--s-4) var(--s-5);
+  display: flex;
+  gap: var(--s-3);
+  align-items: center;
+  border-bottom: 1px solid var(--border-soft);
+  flex-wrap: wrap;
 }
-@media (max-height: 800px) {
-  .local-card {
-    display: none;
-  }
-  .sidebar-bottom {
-    padding-top: 12px;
-  }
+.detail-heading > div {
+  flex: 1;
+  min-width: 200px;
+}
+.detail-heading p {
+  font-size: var(--fs-12);
+  color: var(--ink-muted);
+  margin-top: 2px;
+}
+.message-body {
+  padding: var(--s-5);
+}
+.message-body > * + * {
+  margin-top: var(--s-4);
 }
-/* v0.2: actions and ledger keep controls readable in compact windows. */
-.row-actions {
+.message-label {
+  font-size: var(--fs-12);
+  letter-spacing: 1.4px;
+  font-weight: 650;
+  color: var(--ink-muted);
   display: flex;
-  gap: 12px;
+  align-items: center;
+  gap: 6px;
+  margin-bottom: var(--s-2);
 }
-.movement-reason {
-  display: block;
-  max-width: 370px;
-  white-space: normal;
-  margin-top: 4px;
-  color: #5f6f74;
+.message-label svg {
+  width: 14px;
+  height: 14px;
+  color: var(--accent);
+}
+.message-bubble {
+  background: var(--crema);
+  border: 1px solid var(--border-soft);
+  padding: var(--s-4) var(--s-5);
+  border-radius: 4px var(--r-lg) var(--r-lg) var(--r-lg);
+  font-size: var(--fs-15);
+  line-height: 1.6;
+  color: var(--ink);
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+  max-width: 72ch;
+}
+.message-bubble.outgoing {
+  background: var(--pistacho-claro);
+  border-color: #d8e2c9;
+  border-radius: var(--r-lg) 4px var(--r-lg) var(--r-lg);
+}
+.interpretation {
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
+  padding: var(--s-4) var(--s-5);
+  background: var(--surface);
+}
+.interpretation h3 {
+  margin-top: var(--s-3);
+}
+.interpretation .message-label + h3 {
+  margin-top: 0;
+}
+.interpretation p {
+  font-size: var(--fs-14);
+  line-height: 1.55;
+  color: var(--ink-soft);
+  margin: var(--s-1) 0 var(--s-2);
+}
+.interpretation .muted {
+  margin-top: var(--s-2);
 }
-.order-row > div:last-child {
+.reply-reading {
+  margin-top: var(--s-4);
+  padding-top: var(--s-3);
+  border-top: 1px solid var(--border-soft);
+}
+.reply-reading h3 {
   display: flex;
-  gap: 8px;
+  align-items: center;
+  gap: var(--s-2);
   flex-wrap: wrap;
 }
-.heading-actions {
-  flex-wrap: wrap;
+.message-actions {
+  padding-top: var(--s-1);
 }
-#form-error:empty {
-  display: none;
+
+/* ---------- Proveedores ---------- */
+.supplier-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
+  gap: var(--s-4);
 }
-#form-error:not(:empty) {
-  padding: 12px;
-  background: #fbe7ec;
-  border-radius: 7px;
+.supplier-card {
+  padding: var(--s-5);
+  display: flex;
+  flex-direction: column;
+}
+.supplier-card h2 {
+  font-family: var(--font-serif);
+  font-weight: 500;
+  font-size: var(--fs-22);
+  color: var(--ink-title);
+}
+.supplier-card > p {
+  font-size: var(--fs-13);
+  color: var(--ink-muted);
+  margin: var(--s-1) 0 var(--s-4);
+}
+.supplier-info {
+  display: flex;
+  gap: var(--s-2);
+  align-items: center;
+  color: var(--ink-soft);
+  font-size: var(--fs-13);
+  margin: var(--s-2) 0;
+}
+.supplier-info svg {
+  width: 16px;
+  height: 16px;
+  color: var(--ink-muted);
 }
 .supplier-card-footer {
-  gap: 8px;
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--s-2);
+  flex-wrap: wrap;
+  margin-top: auto;
+  padding-top: var(--s-4);
+  border-top: 1px solid var(--border-soft);
+}
+.supplier-card-footer .btn:first-child {
+  margin-top: var(--s-2);
+}
+.supplier-card-footer .btn {
+  margin-top: var(--s-2);
 }
 
-.message-filters {
-  display: grid;
-  grid-template-columns: 1.4fr 1fr 1fr;
-  gap: 16px;
-  margin: 20px 0;
+/* ---------- Actividad ---------- */
+.activity-panel {
+  padding: var(--s-2) var(--s-5) var(--s-3);
 }
-.message-filters .field {
-  margin: 0;
+.activity-panel .panel-heading {
+  padding-left: 0;
+  padding-right: 0;
 }
-.message-actions {
-  flex-wrap: wrap;
+.activity-row {
+  display: flex;
+  gap: var(--s-3);
+  padding: var(--s-3) 0;
+  border-bottom: 1px solid var(--border-soft);
+}
+.activity-row:last-child {
+  border: 0;
+}
+.activity-row > div {
+  min-width: 0;
+}
+.activity-dot {
+  width: 10px;
+  height: 10px;
+  flex: none;
+  border: 2px solid var(--accent);
+  border-radius: 50%;
+  margin-top: 6px;
+}
+.activity-row p {
+  font-size: var(--fs-14);
+  color: var(--ink);
+  max-width: 80ch;
+}
+.activity-row small {
+  display: block;
+  margin-top: 2px;
 }
 
-.tracking-intro {
+/* ---------- Configuración e IA ---------- */
+.settings-grid {
   display: grid;
-  grid-template-columns: 1.5fr 1fr;
-  gap: 32px;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: var(--s-5);
   align-items: start;
-  margin: 32px 0 24px;
 }
-.tracking-intro h2 {
-  font-size: 26px;
-  margin: 10px 0;
+.settings-card {
+  padding: var(--s-5);
 }
-.tracking-intro p {
-  line-height: 1.65;
-  max-width: 640px;
-  color: #3a4a50;
+.settings-card > .stat-icon {
+  margin-bottom: var(--s-3);
 }
-.tracking-example {
-  background: #eaf0e5;
-  border-radius: 12px;
-  padding: 20px;
+.settings-card h2 {
+  margin-bottom: var(--s-2);
 }
-.tracking-example p {
-  margin: 8px 0;
+.settings-card > p {
+  font-size: var(--fs-14);
+  line-height: 1.55;
+  color: var(--ink-soft);
+  margin: var(--s-2) 0;
 }
-.tracking-tabs {
+.settings-card > p:has(> .btn) {
   display: flex;
-  gap: 10px;
-  margin: 20px 0;
+  align-items: center;
+  gap: var(--s-2);
+  flex-wrap: wrap;
+  max-width: none;
 }
-.delivery-list {
-  display: grid;
-  gap: 20px;
+.settings-card > p > .btn {
+  min-height: var(--control-sm);
+  padding: 0 var(--s-3);
 }
-.delivery-card {
-  background: #fffdf9;
-  border: 1px solid #dce2d4;
-  border-radius: 14px;
-  overflow: hidden;
+.settings-card > .pill {
+  margin-bottom: var(--s-2);
 }
-.delivery-card > header {
-  display: flex;
-  justify-content: space-between;
-  align-items: center;
-  padding: 22px 24px 12px;
+.settings-card > .btn {
+  margin: var(--s-2) 0;
 }
-.delivery-card h3 {
-  font-size: 22px;
-  margin: 8px 0;
+.settings-card > .btn + .btn {
+  margin-left: var(--s-2);
 }
-.delivery-progress {
-  display: flex;
-  justify-content: space-between;
-  gap: 20px;
-  padding: 0 24px 18px;
-  color: #3a4a50;
-  font-size: 13px;
+.settings-card > .field {
+  margin-top: var(--s-3);
 }
-.delivery-table {
-  width: 100%;
-  border-collapse: collapse;
+.settings-card > .fineprint:last-child {
+  margin-bottom: 0;
 }
-.delivery-table th {
-  text-align: left;
-  font-size: 12px;
-  background: #f3f5ee;
-  padding: 12px 24px;
-  color: #52604a;
+.lock-panel {
+  max-width: 520px;
 }
-.delivery-table td {
-  padding: 16px 24px;
-  border-bottom: 1px solid #edf0e7;
+.path-label {
+  font-size: var(--fs-12);
+  letter-spacing: 1.4px;
+  font-weight: 650;
+  color: var(--ink-muted);
+  display: block;
+  margin: var(--s-4) 0 var(--s-2);
 }
-.delivery-table small,
-.delivery-next small {
+.path {
   display: block;
-  color: #62705a;
-  margin-top: 5px;
+  word-break: break-all;
+  background: var(--crema-hundida);
+  padding: var(--s-3);
+  border-radius: var(--r-md);
+  color: var(--ink-soft);
+  font-size: var(--fs-12);
+  white-space: pre-wrap;
+  margin: var(--s-2) 0;
 }
-.delivery-next {
+.feature-list {
+  padding: 0;
+  list-style: none;
+  color: var(--ink-soft);
+  font-size: var(--fs-13);
+  margin: var(--s-3) 0;
+}
+.feature-list li {
   display: flex;
-  justify-content: space-between;
-  gap: 20px;
-  align-items: center;
-  padding: 20px 24px;
-  background: #fdfaf4;
-  font-size: 13px;
+  gap: var(--s-2);
+  align-items: flex-start;
+  margin: var(--s-2) 0;
+  line-height: 1.5;
 }
-.delivery-next strong {
-  font-weight: 500;
+.feature-list svg {
+  width: 16px;
+  height: 16px;
+  color: var(--pistacho);
+  margin-top: 2px;
 }
-.table-scroll {
-  overflow: auto;
+.steps {
+  padding-left: 20px;
+  line-height: 1.55;
+  color: var(--ink-soft);
+  font-size: var(--fs-14);
 }
-.ai-result {
-  white-space: pre-wrap;
+.steps li {
+  margin-bottom: var(--s-2);
+  padding-left: 4px;
+}
+.steps strong {
+  color: var(--ink);
+}
+.photo-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
+  gap: var(--s-4);
+  margin-top: var(--s-4);
+}
+.photo-grid figure {
+  margin: 0;
+  min-width: 0;
+  border: 1px solid var(--border);
+  border-radius: var(--r-md);
+  overflow: hidden;
+  background: var(--surface);
+}
+.photo-grid img {
+  width: 100%;
+  height: 132px;
+  object-fit: cover;
+  display: block;
+  border-bottom: 1px solid var(--border-soft);
+  background: var(--crema-hundida);
+}
+.photo-grid figcaption {
+  font-size: var(--fs-13);
+  color: var(--ink-soft);
   overflow-wrap: anywhere;
-  line-height: 1.7;
-  padding: 20px;
-  background: #eef3e6;
-  border-radius: 12px;
+  padding: var(--s-3);
+  line-height: 1.5;
+}
+.photo-grid figcaption strong {
+  display: block;
+  color: var(--ink);
+}
+.photo-grid small {
+  display: block;
+}
+.photo-grid small:empty {
+  display: none;
+}
+.photo-grid figcaption .row-actions {
+  margin-top: var(--s-2);
+}
+.photo-grid figcaption .btn {
+  min-height: var(--control-sm);
+  padding: 0 var(--s-3);
+  box-shadow: none;
 }
 .ai-editor {
-  min-height: 180px;
-  resize: vertical;
+  min-height: 200px;
   width: 100%;
 }
-@media (max-width: 1100px) {
-  .tracking-intro {
-    grid-template-columns: 1fr;
-  }
-  .delivery-next {
-    align-items: start;
-    flex-direction: column;
-  }
-  .delivery-table td,
-  .delivery-table th {
-    padding: 12px;
-  }
+.settings-card > small {
+  display: block;
+  margin-top: var(--s-2);
 }
-
-.delivery-next {
-  color: #35604a;
-  margin: 0;
-  max-width: none;
+.settings-card > p[role="status"] {
+  font-size: var(--fs-13);
+  color: var(--ink-muted);
+}
+.ai-result {
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+  line-height: 1.55;
+  padding: var(--s-4) var(--s-5);
+  background: var(--accent-soft);
+  border-radius: var(--r-lg);
+  margin: var(--s-3) 0;
+  font-size: var(--fs-14);
+}
+.ai-result > strong {
+  font-size: var(--fs-18);
+  color: var(--ink-title);
+  display: block;
+  margin-bottom: var(--s-2);
+}
+.ai-result p {
+  margin: var(--s-2) 0;
 }
 .ai-result blockquote {
-  margin: 12px 0;
-  padding-left: 14px;
-  border-left: 3px solid #5c7f63;
+  margin: var(--s-3) 0;
+  padding-left: var(--s-3);
+  border-left: 3px solid var(--accent);
+  color: var(--ink-soft);
+}
+.ai-result small {
+  display: block;
+  margin-top: var(--s-2);
 }
-
 .ai-warning {
-  border-left: 3px solid #b04a62;
-  padding-left: 12px;
-  color: #7d2f45;
+  border-left: 3px solid var(--rosa);
+  padding-left: var(--s-3);
+  color: var(--rosa-texto);
 }
 .ai-math {
-  border-top: 1px solid #d6dfd0;
-  margin-top: 18px;
-  padding-top: 14px;
+  border-top: 1px solid rgba(30, 42, 47, 0.12);
+  margin-top: var(--s-3);
+  padding-top: var(--s-3);
+}
+.ai-chat {
+  grid-column: 1 / -1;
 }
-
 .ai-chat-log {
   display: grid;
-  gap: 10px;
+  gap: var(--s-2);
   max-height: 360px;
   overflow: auto;
-  padding: 4px 0;
+  padding: var(--s-1) 0;
+  margin-bottom: var(--s-3);
 }
 .ai-chat-msg {
-  padding: 12px 14px;
-  border-radius: 12px;
-  background: #f5f0e6;
+  padding: var(--s-3) var(--s-4);
+  border-radius: var(--r-md) var(--r-lg) var(--r-lg) var(--r-lg);
+  background: var(--crema-hundida);
+  max-width: 72ch;
+}
+.ai-chat-msg.user {
+  justify-self: end;
+  border-radius: var(--r-lg) var(--r-md) var(--r-lg) var(--r-lg);
 }
 .ai-chat-msg.assistant {
-  background: #eef3e6;
+  background: var(--accent-soft);
+}
+.ai-chat-msg strong {
+  font-size: var(--fs-12);
+  color: var(--ink-muted);
+  letter-spacing: 0.5px;
+  text-transform: uppercase;
 }
 .ai-chat-msg p {
-  margin: 4px 0 0;
+  margin: var(--s-1) 0 0;
   white-space: pre-wrap;
   overflow-wrap: anywhere;
+  max-width: none;
 }
 
-.reply-reading {
-  margin-top: 14px;
-  padding-top: 12px;
-  border-top: 1px solid #d6dfd0;
-}
-.reply-reading h3 {
+/* ---------- WhatsApp ---------- */
+.wa-reading {
   display: flex;
+  gap: var(--s-2);
   align-items: center;
-  gap: 8px;
   flex-wrap: wrap;
+  margin: var(--s-2) 0;
 }
-.order-replies {
-  padding: 12px 20px 0;
-  font-size: 0.92rem;
+.wa-reading small {
+  flex: 1 1 200px;
 }
-.order-replies p {
-  margin: 6px 0;
+.message-bubble .btn {
+  margin-top: var(--s-2);
+}
+.settings-card > .message-bubble {
+  margin-top: var(--s-3);
+  max-width: none;
 }
-.home-notice strong + span {
+.message-bubble > strong {
+  color: var(--ink);
+}
+.message-bubble > small {
+  display: inline;
+}
+.message-bubble > p {
+  margin-top: var(--s-1);
+}
+.diag {
+  font-size: var(--fs-12);
+  line-height: 1.5;
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+  background: var(--crema-hundida);
+  color: var(--ink-soft);
+  padding: var(--s-3);
+  border-radius: var(--r-md);
+  max-height: 220px;
+  overflow: auto;
+  margin: var(--s-3) 0 0;
+}
+.settings-card > img[alt="QR para vincular WhatsApp"] {
   display: block;
+  margin: var(--s-4) 0;
+  border: 1px solid var(--border);
+  border-radius: var(--r-md);
+  background: #fff;
 }
+
+/* ---------- Producción y recetas ---------- */
 .production-card {
-  border: 1px solid #e6dfd2;
-  border-radius: 14px;
-  padding: 16px;
-  margin-bottom: 14px;
-  background: #fbfaf6;
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
+  padding: var(--s-4) var(--s-5) var(--s-5);
+  margin: 0 var(--s-5) var(--s-4);
+  background: var(--crema);
 }
 .production-card header {
   display: flex;
   justify-content: space-between;
-  align-items: center;
-  gap: 12px;
-  margin-bottom: 10px;
+  align-items: flex-start;
+  gap: var(--s-3);
+  margin-bottom: var(--s-3);
+  flex-wrap: wrap;
 }
-.inline-input {
-  width: 110px;
-  padding: 6px 8px;
-  border: 1px solid #d6dfd0;
-  border-radius: 8px;
-  font: inherit;
+.production-card header strong {
+  display: block;
+  font-size: var(--fs-18);
+  font-family: var(--font-serif);
+  font-weight: 500;
+  color: var(--ink-title);
 }
-.inline-input.wide {
-  width: 100%;
+.production-card header small {
+  display: block;
+  margin-top: 2px;
+}
+.production-card .table-scroll {
+  border: 1px solid var(--border-soft);
+  border-radius: var(--r-md);
+  background: var(--surface);
+}
+.production-card .delivery-table th,
+.production-card .delivery-table td {
+  padding: var(--s-3) var(--s-4);
+}
+.production-card > p {
+  margin-top: var(--s-4);
+  display: flex;
+  align-items: center;
+  gap: var(--s-2);
+  flex-wrap: wrap;
+  max-width: none;
+}
+.production-card > .field {
+  margin: var(--s-3) 0 var(--s-4);
 }
 .recipe-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
-  gap: 14px;
+  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
+  gap: var(--s-4);
+  padding: 0 var(--s-5) var(--s-5);
+}
+.recipe-grid > .empty {
+  grid-column: 1 / -1;
 }
 .recipe-card {
-  border: 1px solid #e6dfd2;
-  border-radius: 14px;
-  padding: 16px;
-  background: #fff;
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
+  padding: var(--s-4) var(--s-5) var(--s-5);
+  background: var(--surface);
+  display: flex;
+  flex-direction: column;
+  gap: var(--s-2);
+}
+.recipe-card h3 {
+  font-family: var(--font-serif);
+  font-weight: 500;
+  font-size: var(--fs-18);
+  color: var(--ink-title);
 }
 .recipe-card ul {
-  margin: 10px 0;
+  margin: var(--s-1) 0;
   padding-left: 18px;
+  color: var(--ink-soft);
+  font-size: var(--fs-14);
 }
-.ingredient-row {
-  display: grid;
-  grid-template-columns: 1fr 120px auto;
-  gap: 8px;
-  align-items: center;
-  margin-bottom: 8px;
+.recipe-card li {
+  margin: 2px 0;
 }
-
-.wa-reading {
-  display: flex;
-  gap: 8px;
-  align-items: center;
-  flex-wrap: wrap;
-  margin: 6px 0;
+.recipe-card .row-actions {
+  margin-top: auto;
+  padding-top: var(--s-2);
 }
-
-.message-bubble.outgoing {
-  background: #e6eedc;
-  border-left: 3px solid #5c7f63;
+.panel[data-sales] > .field {
+  margin: 0 var(--s-5) var(--s-3);
 }
-
-.diag {
-  font-size: 0.8rem;
-  white-space: pre-wrap;
-  overflow-wrap: anywhere;
-  background: #f5f0e6;
-  padding: 10px 12px;
-  border-radius: 10px;
-  max-height: 220px;
-  overflow: auto;
+.panel[data-sales] > .row-actions,
+.panel[data-sales] > .muted {
+  padding: var(--s-4) var(--s-5);
 }
-
-.field.short {
-  max-width: 220px;
+.panel[data-sales] > .muted {
+  display: block;
+}
+.panel > .empty.compact {
+  border-top: 1px solid var(--border-soft);
 }
 
-.steps {
-  padding-left: 20px;
-  line-height: 1.6;
+/* ---------- Documentos ---------- */
+.doc-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
+  gap: var(--s-4);
+  padding: 0 var(--s-5) var(--s-5);
 }
-.steps li {
-  margin-bottom: 6px;
+.doc-card {
+  display: grid;
+  grid-template-columns: 76px minmax(0, 1fr);
+  gap: var(--s-3) var(--s-4);
+  border: 1px solid var(--border);
+  border-radius: var(--r-lg);
+  padding: var(--s-4);
+  background: var(--surface);
+  min-width: 0;
+  align-content: start;
 }
-
-.lock-panel {
-  max-width: 480px;
+.doc-thumb {
+  width: 76px;
+  height: 76px;
+  border-radius: var(--r-md);
+  overflow: hidden;
+  background: var(--crema-hundida);
+  border: 1px solid var(--border-soft);
+  display: grid;
+  place-items: center;
 }
-
-/* Identidad: marca del negocio en la barra lateral */
-.workspace-icon {
-  background: #35604a;
-  color: #fffdf9;
-  font-family: Georgia, serif;
-  font-weight: 500;
+.doc-thumb img {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  display: block;
 }
-.workspace small {
-  color: #8d9490;
+.doc-pdf {
+  font-weight: 700;
+  color: var(--rosa-texto);
+  letter-spacing: 1px;
+  font-size: var(--fs-13);
+}
+.doc-body {
+  min-width: 0;
+  display: grid;
+  gap: var(--s-1);
+  align-content: start;
 }
-.brand-place {
+.doc-body > strong {
+  font-size: var(--fs-14);
+  color: var(--ink);
+  overflow-wrap: anywhere;
+}
+.doc-body > small {
   display: block;
-  font-size: 10px;
-  color: #8fb07a;
-  letter-spacing: 0.4px;
 }
-.btn.primary {
-  box-shadow: 0 6px 14px rgba(31, 78, 95, 0.18);
+.doc-tags {
+  display: flex;
+  gap: 6px;
+  flex-wrap: wrap;
+  margin-top: var(--s-1);
 }
-.nav-item.active {
-  box-shadow: inset 3px 0 0 #c85c73;
+.doc-suggestion {
+  grid-column: 1 / -1;
+  background: var(--accent-soft);
+  border-radius: var(--r-md);
+  padding: var(--s-3) var(--s-4);
+  font-size: var(--fs-13);
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--s-3);
+  flex-wrap: wrap;
+  color: var(--ink-soft);
+}
+.doc-suggestion > span {
+  flex: 1 1 200px;
+  min-width: 0;
+  line-height: 1.5;
+}
+.doc-suggestion strong {
+  color: var(--ink);
+}
+.doc-suggestion .muted {
+  display: block;
+  margin-top: 2px;
 }
-.stat-icon.coral {
-  background: #fbe7ec;
-  color: #b04a62;
+.doc-suggestion .btn {
+  flex: none;
+  min-height: var(--control-sm);
+  padding: 0 var(--s-3);
+}
+.doc-card > .muted {
+  grid-column: 1 / -1;
+}
+.doc-actions {
+  grid-column: 1 / -1;
+  padding-top: var(--s-2);
+  border-top: 1px solid var(--border-soft);
+}
+.doc-actions .btn {
+  min-height: var(--control-sm);
+  padding: 0 var(--s-3);
+  box-shadow: none;
 }
 
-/* Pantalla de inicio: arte + gelato */
+/* ---------- Pantalla de inicio: arte + gelato ---------- */
 #splash {
   position: fixed;
   inset: 0;
   z-index: 50;
   display: grid;
   place-items: center;
-  background: #fbf7f0;
-  color: #1e2a2f;
+  background: var(--crema);
+  color: var(--tinta);
   pointer-events: none;
-  transition: opacity 0.6s ease;
+  transition: opacity 0.5s ease;
 }
 #splash.hide {
   opacity: 0;
 }
 .splash-inner {
   text-align: center;
-  animation: splash-in 0.7s ease both;
+  animation: splash-in 0.6s ease both;
 }
 .splash-name {
-  font-family: Georgia, "Times New Roman", serif;
+  font-family: var(--font-serif);
   font-size: 56px;
   letter-spacing: -1px;
-  color: #35604a;
+  color: var(--pistacho);
   line-height: 1;
 }
 .splash-tag {
-  margin-top: 10px;
-  font-size: 13px;
+  margin-top: var(--s-3);
+  font-size: var(--fs-13);
   letter-spacing: 4px;
   text-transform: uppercase;
-  color: #6b7680;
+  color: var(--ink-muted);
 }
 .splash-strokes {
   display: flex;
-  gap: 10px;
+  gap: var(--s-3);
   justify-content: center;
-  margin-top: 26px;
+  margin-top: var(--s-5);
 }
 .splash-strokes i {
   display: block;
@@ -2121,9 +2778,9 @@ dialog::backdrop {
   background: #f1cf7e;
 }
 .splash-place {
-  margin-top: 18px;
-  font-size: 12px;
-  color: #8d9490;
+  margin-top: var(--s-4);
+  font-size: var(--fs-13);
+  color: var(--ink-muted);
 }
 @keyframes splash-in {
   from {
@@ -2135,60 +2792,169 @@ dialog::backdrop {
     transform: none;
   }
 }
-@media (prefers-reduced-motion: reduce) {
-  .splash-inner {
-    animation: none;
-  }
-}
 
-.doc-grid {
-  display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
-  gap: 14px;
-  padding: 0 20px 20px;
-}
-.doc-card {
-  display: flex;
-  gap: 12px;
-  border: 1px solid #ebe4d8;
-  border-radius: 14px;
-  padding: 12px;
-  background: #fffdf9;
-}
-.doc-thumb {
-  width: 84px;
-  height: 84px;
-  flex: none;
-  border-radius: 10px;
-  overflow: hidden;
-  background: #f5f0e6;
-  display: grid;
-  place-items: center;
+/* ---------- Adaptación a ventanas más estrechas y bajas ---------- */
+@media (min-width: 1600px) {
+  .hero {
+    min-height: 280px;
+  }
+  .hero-art {
+    right: 8%;
+    transform: scale(1.08);
+    transform-origin: right center;
+  }
+  .hero h2 {
+    font-size: 38px;
+  }
 }
-.doc-thumb img {
-  width: 100%;
-  height: 100%;
-  object-fit: cover;
+@media (max-width: 1280px) {
+  :root {
+    --sidebar: 216px;
+    --content-pad: 24px;
+  }
+  .sidebar {
+    padding-left: var(--s-3);
+    padding-right: var(--s-3);
+  }
+  .brand-row {
+    font-size: 20px;
+  }
+  .brand small {
+    letter-spacing: 1.2px;
+  }
+  h1 {
+    font-size: 28px;
+  }
+  .hero-art {
+    right: 0;
+    width: 340px;
+  }
+  .hero-copy {
+    max-width: 62%;
+  }
+  .stat {
+    padding: var(--s-3) var(--s-3);
+    gap: var(--s-2);
+  }
+  .stat-icon {
+    width: 36px;
+    height: 36px;
+  }
+  .stat strong {
+    font-size: 28px;
+  }
+  .stat .money-value {
+    font-size: 22px;
+  }
+  .dashboard-grid {
+    grid-template-columns: minmax(0, 1.6fr) minmax(280px, 1fr);
+  }
+  th,
+  td,
+  .delivery-table th,
+  .delivery-table td {
+    padding-left: var(--s-4);
+    padding-right: var(--s-4);
+  }
+  .panel-heading {
+    padding: var(--s-4) var(--s-4) var(--s-3);
+  }
+  .purchase-grid {
+    grid-template-columns: minmax(0, 1fr) 280px;
+  }
+  .inbox-layout {
+    grid-template-columns: 300px minmax(0, 1fr);
+  }
+  .search input {
+    width: 160px;
+  }
 }
-.doc-pdf {
-  font-weight: 700;
-  color: #c85c73;
-  letter-spacing: 1px;
+@media (max-width: 1080px) {
+  .hero-art {
+    width: 300px;
+    opacity: 0.75;
+  }
+  .hero-copy {
+    max-width: 66%;
+  }
+  .stats {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+  }
+  .dashboard-grid {
+    grid-template-columns: 1fr;
+  }
+  .right-stack {
+    display: grid;
+    grid-template-columns: 1fr 1fr;
+    align-items: start;
+  }
+  .purchase-grid {
+    grid-template-columns: 1fr;
+  }
+  .inbox-layout {
+    grid-template-columns: 260px minmax(0, 1fr);
+  }
+  .message-filters {
+    grid-template-columns: 1fr 1fr;
+  }
+  .message-filters .field:first-child {
+    grid-column: 1 / -1;
+  }
+  .tracking-intro {
+    grid-template-columns: 1fr;
+  }
+  .doc-grid,
+  .recipe-grid {
+    grid-template-columns: 1fr;
+  }
+  .supplier-grid {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+  }
+  .photo-grid {
+    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
+  }
+  table {
+    white-space: normal;
+  }
+  th,
+  td,
+  .delivery-table th,
+  .delivery-table td {
+    padding-left: var(--s-3);
+    padding-right: var(--s-3);
+  }
+  .pill,
+  .incoming,
+  td strong {
+    white-space: nowrap;
+  }
 }
-.doc-body {
-  min-width: 0;
-  display: grid;
-  gap: 6px;
-  align-content: start;
+@media (max-height: 900px) {
+  .local-card {
+    display: none;
+  }
 }
-.doc-tags {
-  display: flex;
-  gap: 6px;
-  flex-wrap: wrap;
+@media (max-height: 760px) {
+  .sidebar {
+    padding-top: var(--s-4);
+  }
+  .workspace {
+    display: none;
+  }
+  .nav-item {
+    min-height: 34px;
+    padding-top: 6px;
+    padding-bottom: 6px;
+  }
+  .brand {
+    margin-bottom: var(--s-3);
+  }
 }
-.doc-suggestion {
-  background: #eef3e6;
-  border-radius: 10px;
-  padding: 8px 10px;
-  font-size: 12px;
+@media (prefers-reduced-motion: reduce) {
+  * {
+    transition: none !important;
+  }
+  .splash-inner {
+    animation: none;
+  }
 }
diff --git a/src/ui/core.js b/src/ui/core.js
index 5ec28a3..4b37ce2 100644
--- a/src/ui/core.js
+++ b/src/ui/core.js
@@ -199,8 +199,26 @@ function nav(to) {
   filter = "Todos";
   render();
 }
+const pageLabel = {
+  home: "Resumen",
+  stock: "Inventario",
+  orders: "Compras",
+  messages: "Mensajes",
+  whatsapp: "WhatsApp",
+  suppliers: "Proveedores",
+  activity: "Actividad",
+  settings: "Configuración",
+  ai: "IA local",
+  production: "Producción",
+  documents: "Documentos",
+};
 function header(title, description, actions = "") {
-  return `<div class="page-heading"><div><div class="eyebrow">TU NEGOCIO, EN ORDEN</div><h1>${title}</h1><p>${description}</p></div><div class="heading-actions">${actions}</div></div>`;
+  // Rótulo por pantalla: el acento de color lo pone main.page-… en styles.css.
+  const eyebrow =
+    page === "home"
+      ? "TU NEGOCIO, EN ORDEN"
+      : (pageLabel[page] || "").toUpperCase();
+  return `<div class="page-heading"><div><div class="eyebrow"><i class="dot"></i>${esc(eyebrow)}</div><h1>${title}</h1><p>${description}</p></div><div class="heading-actions">${actions}</div></div>`;
 }
 const initials = (name) =>
   (name || "")
@@ -241,7 +259,7 @@ function render() {
   };
   dismissSplash();
   $("#app").innerHTML =
-    `<aside class="sidebar"><a href="#" class="brand" data-nav="home"><span class="brandmark">${icon("ice")}</span><span>gelato<span class="brand-light">stock</span><small>ARTE + GELATO · EN ORDEN</small></span></a><div class="workspace"><div class="workspace-icon">${esc(initials(state.business))}</div><div><strong>${esc(state.business)}</strong><small>${esc(state.place || "Tu negocio, en orden")}</small></div></div><div class="nav-label">MI NEGOCIO</div><nav>${[
+    `<aside class="sidebar"><a href="#" class="brand" data-nav="home"><span class="brand-row"><span class="brandmark">${icon("ice")}</span><span>gelato<span class="brand-light">stock</span></span></span><small>ARTE + GELATO · EN ORDEN</small></a><div class="workspace"><div class="workspace-icon">${esc(initials(state.business))}</div><div><strong>${esc(state.business)}</strong><small>${esc(state.place || "Tu negocio, en orden")}</small></div></div><div class="nav-label">MI NEGOCIO</div><nav>${[
       ["home", "home", "Resumen"],
       ["stock", "box", "Inventario"],
       ["orders", "cart", "Compras"],
@@ -259,7 +277,7 @@ function render() {
       )
       .join(
         "",
-      )}</nav><div class="sidebar-bottom"><div class="local-card">${icon("shield")}<strong>Tu información se queda aquí</strong><p>Datos guardados en este equipo. Sin depender de internet.</p><span><i class="dot"></i> Almacenamiento local</span></div><button data-nav="settings" class="nav-item ${page === "settings" ? "active" : ""}">${icon("settings")}<span>Configuración</span></button><div class="profile"><span class="avatar">${esc(initials(state.business))}</span><div><strong>${esc(state.business)}</strong><small>GelatoStock · v${esc(appVersion)}</small></div></div></div></aside><main><header class="topbar"><div class="breadcrumb">${esc(state.business)} <span>/</span> ${{ home: "Resumen", stock: "Inventario", orders: "Compras", messages: "Mensajes", whatsapp: "WhatsApp", suppliers: "Proveedores", activity: "Actividad", settings: "Configuración", ai: "IA local", production: "Producción", documents: "Documentos" }[page]}</div><div class="top-right"><span class="local-status"><i class="dot"></i> Modo local</span><button class="icon-button" aria-label="Ver mensajes" data-nav="messages">${icon("bell")}${unread ? '<i class="notification-dot"></i>' : ""}</button><span class="avatar small">${esc(initials(state.business))}</span></div></header><div class="content">${views[page]()}</div><footer>Hecho para ${esc(state.business)}${state.place ? ", " + esc(state.place) : ""}.<span>Pedidos reales solo por WhatsApp con tu confirmación</span></footer></main>`;
+      )}</nav><div class="sidebar-bottom"><div class="local-card">${icon("shield")}<strong>Tu información se queda aquí</strong><p>Datos guardados en este equipo. Sin depender de internet.</p><span><i class="dot"></i> Almacenamiento local</span></div><button data-nav="settings" class="nav-item ${page === "settings" ? "active" : ""}">${icon("settings")}<span>Configuración</span></button><div class="profile"><span class="avatar">${esc(initials(state.business))}</span><div><strong>${esc(state.business)}</strong><small>GelatoStock · v${esc(appVersion)}</small></div></div></div></aside><main class="page-${esc(page)}"><header class="topbar"><div class="breadcrumb">${esc(state.business)} <span>/</span> ${esc(pageLabel[page] || "")}</div><div class="top-right"><span class="local-status"><i class="dot"></i> Modo local</span><button class="icon-button" aria-label="Ver mensajes" data-nav="messages">${icon("bell")}${unread ? '<i class="notification-dot"></i>' : ""}</button><span class="avatar small">${esc(initials(state.business))}</span></div></header><div class="content">${views[page]()}</div><footer>Hecho para ${esc(state.business)}${state.place ? ", " + esc(state.place) : ""}.<span>Pedidos reales solo por WhatsApp con tu confirmación</span></footer></main>`;
 }
 async function refreshWhatsApp() {
   if (page !== "whatsapp" || waBusy || document.querySelector("#modal")?.open)
diff --git a/src/ui/views.js b/src/ui/views.js
index 58a4b71..f48edcc 100644
--- a/src/ui/views.js
+++ b/src/ui/views.js
@@ -138,7 +138,8 @@ function documentCard(ph) {
         .join(" · ")
     : "";
   const isPdf = ph.mime === "application/pdf";
-  return `<article class="doc-card"><div class="doc-thumb">${isPdf ? `<span class="doc-pdf">PDF</span>` : `<img src="${esc(ph.data || "/api/photos/" + ph.id)}" alt="${esc(ph.name)}">`}</div><div class="doc-body"><strong>${esc(ph.name)}</strong><small>${esc(ph.supplier ? supplier(ph.supplier).name : "Sin proveedor")} · ${esc(ph.documentDate || ph.at.slice(0, 10))} · ${ph.source === "whatsapp" ? "WhatsApp" : "Foto"}</small><div class="doc-tags">${pill(docTypeLabel(ph.docType), ph.docType ? "sage" : "neutral")}${order ? pill(order.number, "lavender") : pill("Sin pedido", "neutral")}</div>${sug && sugText ? `<div class="doc-suggestion"><strong>Propuesta por reglas:</strong> ${sugText}. <span class="muted">${esc(sug.reason)}</span> ${btn("Aceptar", "applySuggestion", "primary", `data-id="${esc(ph.id)}"`)}</div>` : sug ? `<div class="muted">${esc(sug.reason)}</div>` : ""}<div class="row-actions">${btn("Organizar", "organizePhoto", "secondary", `data-id="${esc(ph.id)}"`)}${btn(order ? "Cambiar pedido" : "Vincular pedido", "linkDocument", "secondary", `data-id="${esc(ph.id)}"`)}${order ? btn("Desvincular", "unlinkDocument", "secondary", `data-id="${esc(ph.id)}"`) : ""}${ph.ocrText ? btn("Revisar texto con IA", "aiPhoto", "secondary", `data-id="${esc(ph.id)}"`) : ""}${btn("Dónde está", "documentPath", "secondary", `data-id="${esc(ph.id)}"`)}</div></div></article>`;
+  // Tarjeta en rejilla: miniatura + datos arriba; propuesta y acciones ocupan todo el ancho.
+  return `<article class="doc-card"><div class="doc-thumb">${isPdf ? `<span class="doc-pdf">PDF</span>` : `<img src="${esc(ph.data || "/api/photos/" + ph.id)}" alt="${esc(ph.name)}">`}</div><div class="doc-body"><strong>${esc(ph.name)}</strong><small>${esc(ph.supplier ? supplier(ph.supplier).name : "Sin proveedor")} · ${esc(ph.documentDate || ph.at.slice(0, 10))} · ${ph.source === "whatsapp" ? "WhatsApp" : "Foto"}</small><div class="doc-tags">${pill(docTypeLabel(ph.docType), ph.docType ? "sage" : "neutral")}${order ? pill(order.number, "lavender") : pill("Sin pedido", "neutral")}</div></div>${sug && sugText ? `<div class="doc-suggestion"><span><strong>Propuesta por reglas:</strong> ${sugText}. <span class="muted">${esc(sug.reason)}</span></span>${btn("Aceptar", "applySuggestion", "primary", `data-id="${esc(ph.id)}"`)}</div>` : sug ? `<div class="muted">${esc(sug.reason)}</div>` : ""}<div class="row-actions doc-actions">${btn("Organizar", "organizePhoto", "secondary", `data-id="${esc(ph.id)}"`)}${btn(order ? "Cambiar pedido" : "Vincular pedido", "linkDocument", "secondary", `data-id="${esc(ph.id)}"`)}${order ? btn("Desvincular", "unlinkDocument", "secondary", `data-id="${esc(ph.id)}"`) : ""}${ph.ocrText ? btn("Revisar texto con IA", "aiPhoto", "secondary", `data-id="${esc(ph.id)}"`) : ""}${btn("Dónde está", "documentPath", "secondary", `data-id="${esc(ph.id)}"`)}</div></article>`;
 }
 function documents() {
   const list = state.photos.filter(
@@ -248,7 +249,7 @@ function production() {
       state.recipes
         .map(
           (r) =>
-            `<article class="recipe-card"><h3>${esc(r.name)}</h3><small>Rinde ${num(r.yield)} kg${r.product ? " · terminado: " + esc(product(r.product).name) : " · sin producto terminado"}</small><ul>${r.ingredients.map((i) => `<li>${lineText(i)}</li>`).join("")}</ul>${r.note ? `<p class="muted">${esc(r.note)}</p>` : ""}<div class="row-actions">${btn("Producir", "produce", "primary", `data-recipe="${esc(r.id)}"`)}${btn("Editar", "recipeEditor", "secondary", `data-id="${esc(r.id)}"`)}${btn("Eliminar", "deleteRecipe", "secondary", `data-id="${esc(r.id)}"`)}</div></article>`,
+            `<article class="recipe-card"><h3>${esc(r.name)}</h3><small>Rinde ${num(r.yield)} kg${r.product ? " · terminado: " + esc(product(r.product).name) : " · sin producto terminado"}</small><ul>${r.ingredients.map((i) => `<li>${lineText(i)}</li>`).join("")}</ul>${r.note ? `<p class="muted">${esc(r.note)}</p>` : ""}<div class="row-actions">${btn("Producir", "produce", "primary", `data-recipe="${esc(r.id)}"`)}${btn("Editar", "recipeEditor", "secondary", `data-id="${esc(r.id)}"`)}${btn("Eliminar", "deleteRecipe", "danger", `data-id="${esc(r.id)}"`)}</div></article>`,
         )
         .join("") ||
       '<div class="empty compact">Sin recetas. Crea la primera con «Nueva receta».</div>'
@@ -322,7 +323,7 @@ function orderTracking() {
           return docs.length
             ? `<div class="order-replies"><strong>Documentos vinculados</strong>${docs.map((d) => `<p>${pill(docTypeLabel(d.docType), "sage")} ${esc(d.name)} · ${esc(d.documentDate || d.at.slice(0, 10))}</p>`).join("")}</div>`
             : "";
-        })()}<footer class="delivery-next"><div><strong>${hint}</strong><small>${o.dispatch ? `Enviado por WhatsApp a ${esc(o.dispatch.to)} el ${date(o.dispatch.at)} ${time(o.dispatch.at)}. Pendiente de confirmación del proveedor.` : o.status === "pending" || o.status === "sent" ? "Simulación: no se ha contactado al proveedor." : "Registro de demostración · sin pagos ni mensajes enviados."}</small></div><div class="row-actions">${o.status === "pending" ? btn("Enviar por WhatsApp", "orderWhatsApp", "primary", `data-order="${esc(o.id)}"`) + btn("Simular envío", "send", "secondary", `data-order="${esc(o.id)}"`) + btn("Cancelar pedido", "cancelOrder", "secondary", `data-order="${esc(o.id)}"`) : ["sent", "partial"].includes(o.status) ? btn("Registrar lo que llegó", "receive", "primary", `data-order="${esc(o.id)}"`) : ""}</div></footer></article>`;
+        })()}<footer class="delivery-next"><div><strong>${hint}</strong><small>${o.dispatch ? `Enviado por WhatsApp a ${esc(o.dispatch.to)} el ${date(o.dispatch.at)} ${time(o.dispatch.at)}. Pendiente de confirmación del proveedor.` : o.status === "pending" || o.status === "sent" ? "Simulación: no se ha contactado al proveedor." : "Registro de demostración · sin pagos ni mensajes enviados."}</small></div><div class="row-actions">${o.status === "pending" ? btn("Enviar por WhatsApp", "orderWhatsApp", "primary", `data-order="${esc(o.id)}"`) + btn("Simular envío", "send", "secondary", `data-order="${esc(o.id)}"`) + btn("Cancelar pedido", "cancelOrder", "danger", `data-order="${esc(o.id)}"`) : ["sent", "partial"].includes(o.status) ? btn("Registrar lo que llegó", "receive", "primary", `data-order="${esc(o.id)}"`) : ""}</div></footer></article>`;
       })
       .join("") ||
     '<div class="panel empty compact">' +
@@ -403,7 +404,7 @@ function messages() {
       )
       .join(
         "",
-      )}</select></label></div><section class="inbox-layout"><div class="conversation-list"><div class="conversation-heading">Bandeja de proveedores <span>${list.length} de ${state.messages.length}</span></div>${list.map((x) => `<button class="conversation ${m?.id === x.id ? "selected" : ""}" data-open-message="${x.id}"><span class="supplier-avatar ${supplier(x.supplier).color}">${esc(supplier(x.supplier).initials)}</span><div><div class="conversation-title"><strong>${esc(supplier(x.supplier).name)}</strong><small>${time(x.at)}</small></div><p>${esc(x.text)}</p><span class="mini-status">${x.reviewed ? "Revisado" : priorityLabel[x.priority]} · ${relevanceLabel[x.relevance]}${x.interpretation?.needsReading && !x.reviewed ? " · Leer" : ""}</span></div>${!x.read ? '<i class="unread-dot"></i>' : ""}</button>`).join("")}</div><div class="message-detail">${m ? `<div class="detail-heading"><span class="supplier-avatar ${supplier(m.supplier).color}">${esc(supplier(m.supplier).initials)}</span><div><h2>${esc(supplier(m.supplier).name)}</h2><p>${m.channel === "whatsapp" ? "WhatsApp · " + esc(m.sender || "") : "Mensaje de demostración"} · ${date(m.at)}, ${time(m.at)}</p></div>${pill(m.reviewed ? "Revisado" : priorityLabel[m.priority], m.reviewed ? "sage" : m.priority === "important" ? "peach" : "lavender")}</div><div class="message-body"><div class="message-label">MENSAJE ORIGINAL</div><div class="message-bubble">${esc(m.text)}</div><div class="interpretation"><div class="message-label">${icon("leaf")} LECTURA ASISTIDA POR REGLAS</div><h3>${esc(priorityLabel[m.priority])}</h3><p>${esc(m.reason)}</p><h3>${esc(relevanceLabel[m.relevance])}</h3><p>${esc(m.relevanceReason)}</p>${replyBlock(m)}<div class="muted">${m.order ? "Vinculado a " + esc(state.orders.find((o) => o.id === m.order)?.number) : "Sin pedido vinculado. No se han modificado compras ni stock."}</div></div><div class="message-actions">${btn(aiReplyBusy === m.id ? "Leyendo la respuesta…" : "Segunda lectura con IA local", "aiReadReply", "secondary", `data-id="${esc(m.id)}" ${aiReplyBusy ? "disabled" : ""}`)}${btn("Abrir en IA local", "aiMessage", "secondary", `data-id="${esc(m.id)}"`)}${btn("Corregir lectura", "correctReading", "secondary", `data-id="${esc(m.id)}"`)}${btn(m.reviewed ? "Mensaje revisado" : icon("check") + " Marcar revisado", "review", "primary", `data-id="${m.id}" ${m.reviewed ? "disabled" : ""}`)}${btn("Cambiar prioridad", "priority", "secondary", `data-id="${m.id}"`)}${btn("Corregir relevancia", "relevance", "secondary", `data-id="${m.id}"`)}${btn("Vincular pedido", "link", "secondary", `data-id="${m.id}"`)}</div><p class="fineprint">Revisar un mensaje no acepta sobrecostes ni sustituciones. La conexión real se añadirá en una siguiente etapa.</p></div>` : '<div class="empty">No hay mensajes que coincidan con estos filtros.</div>'}</div></section>`
+      )}</select></label></div><section class="inbox-layout"><div class="conversation-list"><div class="conversation-heading">Bandeja de proveedores <span>${list.length} de ${state.messages.length}</span></div>${list.map((x) => `<button class="conversation ${m?.id === x.id ? "selected" : ""}" data-open-message="${x.id}"><span class="supplier-avatar ${supplier(x.supplier).color}">${esc(supplier(x.supplier).initials)}</span><div><div class="conversation-title"><strong>${esc(supplier(x.supplier).name)}</strong><small>${time(x.at)}</small></div><p>${esc(x.text)}</p><span class="mini-status">${x.reviewed ? "Revisado" : priorityLabel[x.priority]} · ${relevanceLabel[x.relevance]}${x.interpretation?.needsReading && !x.reviewed ? " · Leer" : ""}</span></div>${!x.read ? '<i class="unread-dot"></i>' : ""}</button>`).join("")}</div><div class="message-detail">${m ? `<div class="detail-heading"><span class="supplier-avatar ${supplier(m.supplier).color}">${esc(supplier(m.supplier).initials)}</span><div><h2>${esc(supplier(m.supplier).name)}</h2><p>${m.channel === "whatsapp" ? "WhatsApp · " + esc(m.sender || "") : "Mensaje de demostración"} · ${date(m.at)}, ${time(m.at)}</p></div>${pill(m.reviewed ? "Revisado" : priorityLabel[m.priority], m.reviewed ? "sage" : m.priority === "important" ? "peach" : "lavender")}</div><div class="message-body"><div class="message-label">MENSAJE ORIGINAL</div><div class="message-bubble">${esc(m.text)}</div><div class="interpretation"><div class="message-label">${icon("leaf")} LECTURA ASISTIDA POR REGLAS</div><h3>${esc(priorityLabel[m.priority])}</h3><p>${esc(m.reason)}</p><h3>${esc(relevanceLabel[m.relevance])}</h3><p>${esc(m.relevanceReason)}</p>${replyBlock(m)}<div class="muted">${m.order ? "Vinculado a " + esc(state.orders.find((o) => o.id === m.order)?.number) : "Sin pedido vinculado. No se han modificado compras ni stock."}</div></div><div class="message-actions">${btn(aiReplyBusy === m.id ? "Leyendo la respuesta…" : "Segunda lectura con IA local", "aiReadReply", "secondary", `data-id="${esc(m.id)}" ${aiReplyBusy ? "disabled" : ""}`)}${btn("Abrir en IA local", "aiMessage", "secondary", `data-id="${esc(m.id)}"`)}${btn("Corregir lectura", "correctReading", "secondary", `data-id="${esc(m.id)}"`)}${btn(m.reviewed ? "Mensaje revisado" : icon("check") + " Marcar revisado", "review", "primary", `data-id="${m.id}" ${m.reviewed ? "disabled" : ""}`)}${btn("Cambiar prioridad", "priority", "secondary", `data-id="${m.id}"`)}${btn("Corregir relevancia", "relevance", "secondary", `data-id="${m.id}"`)}${btn("Vincular pedido", "link", "secondary", `data-id="${m.id}"`)}</div><p class="fineprint">Revisar un mensaje no acepta sobrecostes ni sustituciones. La conexión real se añadirá en una siguiente etapa.</p></div>` : '<div class="empty"><h3>No hay mensajes que coincidan con estos filtros.</h3><p>Cambia el filtro «Mostrar», elige otro proveedor o borra la búsqueda.</p></div>'}</div></section>`
   );
 }
 function suppliers() {
@@ -469,10 +470,10 @@ function settings() {
         )
         .map(
           (ph) =>
-            `<figure><img src="${esc(ph.data || "/api/photos/" + ph.id)}" alt="${esc(ph.name)}"><figcaption><strong>${esc(ph.supplier ? supplier(ph.supplier).name : "Sin proveedor")}</strong><small>${esc(ph.documentDate || ph.at.slice(0, 10))}${ph.docType ? " · " + esc(aiTypes[ph.docType] || ph.docType).replace("Posible ", "") : ""}</small>${esc(ph.name)}<small>${esc(ph.note)}</small>${btn("Organizar", "organizePhoto", "secondary", `data-id="${ph.id}"`)}${ph.ocrText ? btn("Revisar texto con IA", "aiPhoto", "secondary", `data-id="${ph.id}"`) : ""}</figcaption></figure>`,
+            `<figure><img src="${esc(ph.data || "/api/photos/" + ph.id)}" alt="${esc(ph.name)}"><figcaption><strong>${esc(ph.supplier ? supplier(ph.supplier).name : "Sin proveedor")}</strong><small>${esc(ph.documentDate || ph.at.slice(0, 10))}${ph.docType ? " · " + esc(aiTypes[ph.docType] || ph.docType).replace("Posible ", "") : ""}</small>${esc(ph.name)}<small>${esc(ph.note)}</small><div class="row-actions">${btn("Organizar", "organizePhoto", "secondary", `data-id="${ph.id}"`)}${ph.ocrText ? btn("Revisar texto con IA", "aiPhoto", "secondary", `data-id="${ph.id}"`) : ""}</div></figcaption></figure>`,
         )
         .join("") || '<p class="muted">Todavía no hay fotos guardadas.</p>'
-    }</div></section><section class="panel settings-card"><span class="stat-icon sage">${icon("shield")}</span><h2>Recetario protegido</h2>${lockInfo?.enabled ? `<p>Las recetas piden contraseña. Estado: <strong>${lockInfo.unlocked ? "desbloqueado (30 min)" : "bloqueado"}</strong>.</p><div class="setting-actions">${btn("Bloquear ahora", "lockNow", "secondary")}${btn("Cambiar contraseña", "lockChange", "secondary")}${btn("Quitar contraseña", "lockRemove", "secondary")}</div>` : `<p>Sin contraseña: cualquiera con la app abierta ve las recetas.</p>${btn("Poner contraseña", "lockSet", "primary")}`}<p class="fineprint">Protege la pantalla y bloquea crear, editar o producir con recetas. No cifra el disco.</p></section><section class="panel settings-card"><h2>Limpieza periódica</h2><p>Borra la actividad y las conversaciones de WhatsApp más antiguas que el plazo elegido (se conservan las 50 entradas más recientes). Los movimientos de stock y las copias no se tocan.</p><label class="field">Conservar<select id="retention-days">${[
+    }</div></section><section class="panel settings-card"><span class="stat-icon sage">${icon("shield")}</span><h2>Recetario protegido</h2>${lockInfo?.enabled ? `<p>Las recetas piden contraseña. Estado: <strong>${lockInfo.unlocked ? "desbloqueado (30 min)" : "bloqueado"}</strong>.</p><div class="setting-actions">${btn("Bloquear ahora", "lockNow", "secondary")}${btn("Cambiar contraseña", "lockChange", "secondary")}${btn("Quitar contraseña", "lockRemove", "danger")}</div>` : `<p>Sin contraseña: cualquiera con la app abierta ve las recetas.</p>${btn("Poner contraseña", "lockSet", "primary")}`}<p class="fineprint">Protege la pantalla y bloquea crear, editar o producir con recetas. No cifra el disco.</p></section><section class="panel settings-card"><h2>Limpieza periódica</h2><p>Borra la actividad y las conversaciones de WhatsApp más antiguas que el plazo elegido (se conservan las 50 entradas más recientes). Los movimientos de stock y las copias no se tocan.</p><label class="field">Conservar<select id="retention-days">${[
       [0, "Sin limpieza automática"],
       [7, "7 días (semanal)"],
       [14, "14 días"],
